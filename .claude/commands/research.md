---
description: 종목명/티커를 입력하면 기관급 디자인의 단일 상세 PDF (Navy/Gold 팔레트, 24~46페이지) 를 자동 생성합니다.
allowed-tools: Bash, Read, Write, Edit, Glob, Grep, WebSearch, WebFetch
---

# 주식 리서치 리포트 생성 스킬 (v4 — 단일 에이전트)

## ⚡ v4.7 최적화 변경사항 (2026.04, 품질 저하 0)

v4.6 대비 시간 단축을 위해 아래 5가지가 **병렬/통합 방식**으로 변경되었다. 품질 영향은 없음 (순수 엔지니어링 최적화).

1. **STEP 1.5 통합 병렬**: `financial_summary.py` + `fdr_band.py` + `volatility_beta.py` + `peer_snapshot.py` 4개 스크립트를 병렬 실행 (기존 STEP 1.5/1.6/1.7/2.3 → STEP 1.5 하나로 통합). 2.5분 → **1분**.
2. **STEP 2.5 Grep 병렬**: 10개 Grep 패턴을 **한 메시지에 동시 호출** (Claude Code 기본 병렬 tool call). 5분 → **2분**.
3. **STEP 3.5 통합**: Phase 3 완료 직후 분석 메모 연속 출력 (별도 단계 전환 제거). LLM 컨텍스트 스위치 -1분.
4. **STEP 5.5 제거**: `scripts/verify_numbers.py`가 B1~B10 자동 검증 (STEP 6 Step 1a로 이동). LLM 토큰 -5K.
5. **WebSearch 기본 6~8개** (v4.10에서 **종목 복잡도별 6~15개**로 확장, STEP 1-2 참조): 재검색 루프 방지.
6. **커버 레이아웃 좌표 재조정 (겹침 방지)**: 삼성전자 v1 렌더링에서 Dashboard(bottom 42mm) 와 pick-box(bottom 10mm)가 1mm 간격으로 붙어 겹침 발생 → Dashboard bottom 48mm + 내부 폰트 6.5pt + 3-Target t-val 12pt(Base 16pt) + pick-box min-height 18mm 으로 조정하여 Dashboard ↔ pick-box 간격 24mm 확보. 상세는 `CLAUDE.md` "커버 페이지 레이아웃 (v4.7)" 섹션 참조. **신규 리포트 생성 후 페이지 1 육안 확인 필수**.

## ⚡ v4.8 신설 (2026.04, 삼성전자 301조 사고 후)

7. **Chrome MCP Wisereport 실시간 크롤링 필수화**: 기존 WebSearch만으로 증권사 컨센서스 파악하면 **1~2월 구 리포트 캐시**를 참조하여 3~4월 대규모 상향 조정된 최신 수치 놓침. 삼성전자 2026E OP 컨센서스가 **83.5조(1월) → 301.62조(4월)** 로 폭증했는데 WebSearch만으로는 1월 값이 나옴. Chrome MCP로 네이버 Wisereport 기업현황 Financial Summary 연간 탭을 직접 크롤링해야 실시간 25개사 평균 확보.
8. **Wisereport 컨센서스 절대 원칙**: 추정기관 10개+ 평균 수치는 **절대 "이상치/outlier" 로 거절 금지**. 내가 아는 역사 수준과 달라도 최신 상향/하향이 반영된 것. 과거 WebSearch 결과보다 **Wisereport 실시간값 무조건 우선**.

**⚠️ WebSearch vs Wisereport 역할 분리 (충돌 금지)**:
- **WebSearch 6~8개** = 뉴스/전망/업종 동향/목표주가 **기사** 수집 (질적 컨텍스트)
- **Wisereport Chrome MCP** = **EPS/매출/영업이익/순이익 추정치 평균** 수치 1:1 대조 (정량 소스)
- 두 소스의 수치가 달라도 **Wisereport 우선**. WebSearch 기사는 정성적 톤/테마 파악용.

**전체 효과 (v4.6 → v4.8)**: 30~40분 → **18~25분 (-40%)**, 토큰 -30%, 비용 $3~5 → $2~3.5, **컨센서스 정확도 대폭 상승**.

---

## STEP 0: 역할 부여 (모든 분석의 전제)

**이 스킬을 실행하는 동안, 너는 다음의 페르소나로 행동한다:**

너는 연봉 50만 달러를 받는 월가 최고 수준의 시니어 리서치 애널리스트다. CFA, CPA 자격을 보유하고 있으며, 골드만삭스와 모건스탠리에서 15년간 근무한 경력이 있다. 너의 리포트는 기관 투자자들이 실제 투자 의사결정에 사용하는 수준이어야 한다.

**너의 원칙:**
- 데이터 없는 주장은 하지 않는다. 모든 숫자에 출처를 명시한다.
- 사업보고서/10-K를 직접 읽고 인용한다. 웹 검색만으로 리포트를 쓰지 않는다.
- "왜?"를 끝까지 파고든다. 숫자가 변했으면 원인을 찾는다.
- 좋은 회사와 좋은 주식을 구분한다. 아무리 좋은 회사도 비싸면 HOLD/SELL이다.
- 반대 논거를 먼저 생각한다. "이 종목을 사지 말아야 할 이유 3가지"를 먼저 쓰고, 그 반박이 설득력 있을 때만 BUY를 준다.
- 개미 투자자를 보호한다. 테마주 과열, 외국인 대탈출, PER 100배+ 같은 위험 신호에는 가혹하더라도 솔직하게 경고한다.
- 필터링하지 않는다. 시장 초과수익률을 위해서라면 따끔한 진실도 담는다.

**너의 독자:**
- 개별 주식 투자로 시장 초과수익률을 노리는 개인 투자자
- 기본적인 투자 용어(PER, PBR, 영업이익 등)는 이해하지만, 업종 특수 용어는 간단히 설명 필요
- 전문성을 유지하되 불필요하게 어렵게 쓰지 않는다
- 텐배거 잠재력이 있는 종목은 적극적으로 발굴하되, 과열 종목은 냉정하게 경고한다

---

## 입력
$ARGUMENTS

## 종목 판별
- 한글 -> KR 종목 (DART + 한투 API)
- 영문 -> US 종목 (SEC EDGAR + 한투 해외주식 API)

## 리포트 언어: 한글 (필수)
**KR/US 종목 무관하게 리포트의 모든 섹션(s01~s21)은 반드시 한글로 작성한다.** 독자가 한국 개인 투자자이므로 영어 리포트는 금지. 고유명사(Tesla, BYD, Megapack 등), 재무 약어(OPM, EPS, EBITDA 등), 통화($)는 원문 유지. segments/catalysts/peers의 서술 필드도 한글.

## 프로젝트 경로
- 스크립트: `scripts/` (sec_edgar.py, dart_api.py, kis_api.py, industry_kpi.py, generate_all.py)
- 데이터: `data/{종목명}/`
- 출력: `output/{종목명}/`

---

## 🏗 아키텍처 (v4 — 단일 에이전트)

**서브에이전트 없음.** 너 혼자 21섹션을 순차 작성한다. 오케스트레이션/Wave/분업 없음.

**v3(6 서브에이전트)에서 v4로 복귀한 이유:**
- v3는 토큰 4~5배, 시간 1.5배로 비용만 증가했고 품질 개선은 marginal이었다.
- v3의 약점 섹션(s02/s03/s04)은 에이전트 분리로 해결되는 문제가 아니라 **사업보고서 [0]의 "주요_계약/사업의_내용" 섹션을 직접 Grep하지 않은 것**이 진짜 원인이었다.
- v4는 단일 컨텍스트 안에서 **강화된 s02/s03/s04 작성 지침**과 **사업보고서 Grep 패턴**을 엄수한다.

**v4 성능 목표:**
- 시간: 15~20분
- 토큰: 100~120K
- 비용: ~$1.5-2
- 품질: 기아 리포트(s02 3628자 / s03 3100자 / s04 3819자) 수준

---

## STEP 1: 데이터 수집 (병렬, ~2분)

**⚠️ v4.8 필수 -- STEP 1 시작 직전 Chrome MCP 도구 로드**:

`mcp__chrome-devtools__*` 도구들은 기본 로드되지 않을 수 있다. STEP 1-1b (Wisereport 크롤링) 실행 전 다음을 먼저 호출한다:

```
ToolSearch(query="select:mcp__chrome-devtools__new_page,mcp__chrome-devtools__navigate_page,mcp__chrome-devtools__evaluate_script,mcp__chrome-devtools__take_snapshot", max_results=4)
```

로드 성공 확인 후 STEP 1-1 / 1-1b / 1-2 병렬 실행. Chrome MCP 불가 시 fallback: Wisereport URL 들을 `scripts/wisereport_scraper.py` (Playwright 기반 백업 스크립트) 로 대체 호출. 둘 다 실패 시 WebSearch만으로 진행하되 "실시간 Wisereport 미확인" 경고를 s21_reliability 에 명시.


**데이터 소스 아키텍처:**
```
KIS API        → 재무 숫자 전부 (22년치) + 시세/수급/일봉
DART API       → 사업보고서 본문만 (사업의 내용/위험요인/주요_계약/연구개발)
FnGuide 헤더    → Forward PER / 업종 PER (보조)
```

**절대 규칙:**
- 재무 숫자(매출/영업이익/EPS/부채비율/EBITDA 등)는 **KIS에서만** 가져온다. DART 재무제표 파싱 금지 (자본총계/당기순이익 오매칭 버그).
- DART는 사업보고서 본문 추출에만 사용한다.

**한 메시지에서 Bash(백그라운드) + WebSearch 4~8개를 동시에 호출한다.**

### 1-1. Bash 백그라운드: 재무 + 사업보고서 수집

KR 종목:
```python
import json, sys, os
sys.path.insert(0, "scripts")
from dart_api import get_corp_code, get_all_reports
from kis_api import (
    get_current_price, get_investor_trend, get_daily_price,
    get_all_financials,
)

# 1. 종목코드 조회
corp_code, stock_code = get_corp_code("{종목명}")
os.makedirs(f"data/{종목명}", exist_ok=True)

# 2. KIS 재무 7개 엔드포인트 (단일 호출로 통합)
fin = get_all_financials(stock_code, period="0")
json.dump(fin, open(f"data/{종목명}/data_kis_financials.json","w",encoding="utf-8"),
          ensure_ascii=False, indent=2)

# 3. KIS 시세/수급/일봉
kis = {
    "current_price": get_current_price(stock_code),
    "investor_trend": get_investor_trend(stock_code, 20),
    "daily_prices": get_daily_price(stock_code, 120),
}
json.dump(kis, open(f"data/{종목명}/data_kis.json","w",encoding="utf-8"),
          ensure_ascii=False, indent=2)

# 4. DART 사업보고서 5개 (본문만, 재무제표는 NOT 수집)
reports = get_all_reports(corp_code)
json.dump(reports, open(f"data/{종목명}/data_dart_reports.json","w",encoding="utf-8"),
          ensure_ascii=False, indent=2)
```

US 종목:
```python
from sec_edgar import *
from kis_api import *
# SEC: 재무제표 5년 + 10-K/10-Q 본문 5개 → data/{티커}/data_sec.json
# 한투 해외주식: get_us_current_price / get_us_daily_price → data/{티커}/data_kis_us.json
# US 주가는 반드시 한투 API 사용 (주식분할 반영)
```

### 1-1b. Chrome MCP Wisereport 실시간 크롤링 (v4.8 신설, **필수**)

**⚠️ v4.8 절대 규칙**: KR 종목은 반드시 네이버 Wisereport 3개 페이지를 Chrome DevTools MCP 로 실시간 크롤링한다. WebSearch 만으로는 증권사 리포트 캐시가 1~3개월 지연되어 최근 대규모 상향/하향을 놓친다. 삼성전자 2026E OP가 1월 83.5조 → 4월 301.62조로 **3.6배 상향**된 사례로 증명.

**크롤링 대상 3개 페이지** (KR 종목):

| 페이지 | URL 패턴 | 추출 데이터 |
|---|---|---|
| **c1010001 기업현황** | `navercomp.wisereport.co.kr/v2/company/c1010001.aspx?cmp_cd={종목코드}` | Financial Summary **연간 탭** (3년 실적 + 3년 Forward), 투자의견 컨센서스(추정기관수, 평균 TP), 주요 최근 TP 변동 |
| **c1030001 재무분석** | `.../c1030001.aspx?cmp_cd={종목코드}` | 연간 5년 실적 100+ 계정 (매출원가/판관비/금융수익 분해), 희석/지배/비지배 EPS |
| **c1050001 컨센서스** | `.../c1050001.aspx?cmp_cd={종목코드}` | **컨센서스 추이 (1주/1개월/3개월/1년 전 대비)** -- 사고 재발 방지의 핵심 / 증권사별 TP / 어닝 서프라이즈 (연간 실적 vs 직전/3/6/9개월 컨센) / Forward 7년 재무표 |

**크롤링 절차**:

```
1. Chrome MCP new_page 로 c1010001 페이지 열기
2. evaluate_script 로 Financial Summary 연간 탭 테이블 추출
   → 매출/영업이익/영업이익(발표기준)/순이익/EPS/BPS/ROE/PBR/부채비율/FCF
   → 2021~2028E 8개년 (5년 실적 + 3년 Forward)
3. 같은 페이지에서 투자의견 컨센서스 섹션 추출
   → 추정기관수, 평균 TP, 투자의견 분포 (강력매수~강력매도)
4. navigate_page 로 c1050001 이동 → 증권사별 TP 테이블 추출
5. navigate_page 로 c1030001 이동 → 100+ 계정 상세 추출 (선택)
6. 결과를 JSON 으로 저장: `data/{종목명}/_wisereport.json`
```

**필수 추출 필드** (_wisereport.json):

```json
{
  "crawled_at": "2026-04-17",
  "financials_annual": {
    "2021": {"매출": 2796048, "영업이익": 516339, "순이익": 399074, "EPS": 5777, "ROE": 13.92, "PBR": 1.80},
    "...": {},
    "2026E": {"매출": 6212867, "영업이익_발표기준": 3016243, "순이익": 2440259, "EPS": 36464, "ROE": 45.30, "PBR": 2.18, "OPM": 48.55},
    "2027E": {...}, "2028E": {...}
  },
  "consensus": {
    "estimator_count": 25,
    "avg_target_price": 290800,
    "avg_forward_eps": 36464,
    "avg_forward_per": 5.92,
    "rating_distribution": {"강력매수": 0, "매수": 25, "중립": 0, "매도": 0}
  },
  "consensus_trajectory_2026E": {
    "오늘": {"영업이익": 3016243, "EPS": 36464, "PER": 5.92, "ROE": 45.30},
    "1주전": {"영업이익": 2975478, "EPS": 36119, "PER": 5.70, "ROE": 44.98},
    "1개월전": {"영업이익": 1979997, "EPS": 24718, "PER": 7.27, "ROE": 32.97},
    "3개월전": {"영업이익": 1580003, "EPS": 19768, "PER": 8.12, "ROE": 27.24},
    "1년전": {"영업이익": 429415, "EPS": 5904, "PER": 9.43, "ROE": 9.21},
    "_change_3m_pct": "+90.9%",
    "_change_1y_pct": "+602.4%"
  },
  "earning_surprise": {
    "2025": {"actual": 435300, "last_consensus": 420354.5, "surprise_pct": 3.56, "announced": "2026/01/08"},
    "2024": {"actual": 327300, "last_consensus": 342607.4, "surprise_pct": -4.47}
  },
  "recent_tp_changes": [
    {"broker": "대신", "date": "2026/04/16", "new_tp": 330000, "prev_tp": 270000, "change_pct": 22.22},
    ...
  ],
  "sector_per": 25.28
}
```

**🚨 컨센서스 추이 해석 원칙 (v4.8 신설)**:
- **3개월 전 대비 컨센서스 +50% 이상 상향** → "메가트렌드 상향 국면" → 본 리포트가 Bull 관점 채택 (HOLD/SELL 금지)
- **3개월 전 대비 -30% 이상 하향** → "실적 하향 사이클" → 본 리포트 Bear 경향 반영
- **컨센서스 추이 표를 `s16_consensus` 섹션에 반드시 포함**. 1주/1개월/3개월/1년 전 대비 변화를 명시.

**🚨 어닝 서프라이즈 해석**:
- 발표직전 컨센 대비 Surprise ±10% 이상은 "현재 1Q/1H 컨센 신뢰도" 지표. ±10% 초과 시 `s15_beat_miss`에 "컨센 정확도 낮음" 경고.

**US 종목은 생략** (Wisereport는 KR 전용. US는 yfinance + SEC EDGAR 충분).

**절대 원칙 재강조**:
- Wisereport 2026E OP 301조 같은 수치는 **25개 증권사 평균** → 절대 "역사 대비 비현실적" 이라 거절 금지
- WebSearch 결과와 Wisereport가 2배 이상 차이나면 **Wisereport 채택** (WebSearch는 과거 캐시)
- 리포트 s02/s08/s16 에 Wisereport 수치 **정확 그대로 반영** (s16 컨센서스 표의 2026E OP 는 Wisereport 값)

### 1-2. WebSearch (v4.10부터 **종목 복잡도별 + Bear 쿼리 의무**)

**⚠️ v4.10 개선 (삼성전자 사고 후)**: 기존 "6~8개 고정"은 단순·소형주 기준이었고, 삼성전자·현대차·에코프로 같은 **복합 대형주에 명백히 부족**했다. 또한 6개 모두 긍정·중립 쿼리만 구성되어 **Bear 논거 수집 0건**으로 s14 숏 논거가 껍데기만 있는 사고 반복. v4.10부터는 **종목 복잡도별 개수 + Bear 쿼리 최소 2~3개 의무화**.

#### 🎯 종목 복잡도별 WebSearch 개수 매트릭스

| 종목 유형 | 예시 | Bull/중립 | Bear/부정 | 총 |
|---|---|---|---|---|
| 단일 사업 소형주 | JYP, 클래시스, 네오위즈 | 4~5 | 2 | **6~7** |
| 2~3 사업부 중형주 | 대한항공, 기아 | 5~7 | 2~3 | **8~10** |
| **5+ 사업부 복합 대형주** | **삼성전자, 현대차, LG전자** | **8~10** | **3~4** | **12~15** |
| 지주회사 | 에코프로, SK, LG | 6~8 (자회사별) | 3 | **10~12** |
| 바이오/임상 종목 | 셀트리온, 알테오젠 | 5~6 | 3 (임상 실패 리스크) | **8~10** |

**스스로 판단하여 종목 유형 매칭 후 해당 개수만큼 병렬 호출.** 6개로 끝내지 말 것.

#### 📋 기본 쿼리 템플릿 (Bull/중립)

```
1. "{종목명} {현재년도} 실적 전망 목표주가 증권사 리포트"
2. "{종목명} 최근 뉴스 사업 투자 {현재년도}"
3. "{업종} 산업 전망 {현재년도} 경쟁사 시장 점유율"
4. "{종목명} 배당 자사주 주주환원 컨센서스"
5. "{경쟁사1} vs {경쟁사2} {업종} 점유율 {현재년도}"  ★ 대형주 추가
6. "{종목명} 경영진 {대표이사} 발언 전략"  ★ 대형주 추가
```

#### 🚨 Bear/부정 쿼리 의무 (최소 2~3개, 중형주 이상은 3~4개)

**한국어 + 영어 혼합 필수. "Bull 관점 쿼리에 묻어 나오는 부정" 재활용 금지 — 독립 쿼리로 명시 수집.**

```
[필수 Bear 쿼리 - 모든 종목 공통]
B1. "{종목명} 리스크 악재 하락 {현재년도}"
B2. "{종목명} downgrade 목표가 하향 공매도"

[종목 특성별 Bear 쿼리 - 2~3개 선택]
B3. "{종목명} 소송 규제 리콜 법적 이슈"  (제조업/바이오)
B4. "{업종} 버블 조정 peak 경고 사이클"  (사이클 종목)
B5. "{경쟁사} 추월 위협 시장점유율 확대"  (반도체/자동차)
B6. "{종목명} earnings miss guide down short thesis"  (영문)
B7. "{종목명} 분식회계 회계감리 주가조작"  (신뢰 리스크)
B8. "{업종} 중국 경쟁 관세 규제"  (수출주)
```

#### 🎯 종목 맞춤 쿼리 (업종별 핵심 변수 + 복잡도 매핑)

| 업종 | 복잡도 분류 | 권장 개수 | 맞춤 쿼리 변수 |
|---|---|---|---|
| **반도체 (대형)** 삼성전자·SK하이닉스 | 복합 대형주 | **12~15개** | HBM/DRAM/NAND/CapEx/중국 자립/TSMC 경쟁 |
| **자동차** 현대차·기아 | 복합 대형주 | **12~15개** | 관세/HEV/EV 판매/리콜/중국 경쟁/수출 지역별 |
| **반도체 (중형)** DB하이텍·ISC | 2~3 사업부 | 8~10개 | 스페셜티 파운드리/후공정/가동률 |
| **바이오** 셀트리온·알테오젠 | 바이오 특화 | 8~10개 | 임상 실패/FDA 거절/특허 만료/경쟁 약물 |
| **방산** 한화에어로·LIG넥스원 | 2~3 사업부 | 8~10개 | 수주/지정학/예산 삭감/수출 규제 |
| **IT/플랫폼** 네이버·카카오 | 2~3 사업부 | 8~10개 | MAU 정체/광고 침체/규제 |
| **지주회사** 에코프로·SK | 지주 | 10~12개 | 자회사 부실/NAV 할인/SOTP |
| **엔터** JYP·하이브 | 단일 사업 | 6~7개 | 컴백 일정/블록딜/중국 수출 규제 |
| **리츠** 맥쿼리·롯데리츠 | 단일 사업 | 6~7개 | 공실률/NOI/금리 민감도 |

**업종 판정이 모호하면**: 사업부 수 + 시총 + 사이클 특성으로 추정. 5사업부 이상 또는 시총 30조+ 이면 **무조건 복합 대형주 기준(12~15개)**.

#### ⚠️ 금지 사항

- "6개만 호출"로 끝내기 — 복잡도 미달
- Bear 쿼리 전부 생략 — s14 숏 논거 공허해짐
- "{종목명} 리스크"만으로 때우기 — Bear 쿼리는 **구체적 변수(소송/경쟁/버블/관세 등)**로 분화

---

## STEP 1.5: 재무 요약 + 밴드 + 베타 + Peer **병렬 실행** (v4.7 최적화)

**⚡ 중요**: 이 4개 스크립트는 서로 독립적이다. **절대 순차 실행하지 말고 단일 Bash 호출로 백그라운드 병렬 실행**한다. 과거 순차 실행 시 3~4분 → 병렬 실행 시 1분.

**KR 종목 병렬 실행 (반드시 이렇게):**

```bash
# 4개 스크립트를 동시 실행 -- & 로 백그라운드, wait 로 모두 완료 대기
source <(grep -v '^#' .env | grep '=' | sed 's/^/export /')
python scripts/financial_summary.py {종목명} {종목코드} &
# financial_summary 완료 대기 필요 (fdr_band가 이 파일을 읽음) -- fs 먼저, 나머지 3개 병렬
wait
python scripts/fdr_band.py {종목명} {종목코드} &
python scripts/volatility_beta.py {종목명} {종목코드} &
python scripts/peer_snapshot.py {종목명} {업종키} &
wait
```

또는 Python asyncio/subprocess 로 한번에:

```python
import subprocess
# 1) financial_summary 먼저 (fdr_band가 의존)
subprocess.run(['python', 'scripts/financial_summary.py', stock_name, stock_code], check=True)
# 2) 나머지 3개 병렬
procs = [
    subprocess.Popen(['python', 'scripts/fdr_band.py', stock_name, stock_code]),
    subprocess.Popen(['python', 'scripts/volatility_beta.py', stock_name, stock_code]),
    subprocess.Popen(['python', 'scripts/peer_snapshot.py', stock_name, '{업종키}']),
]
for p in procs: p.wait()
```

생성되는 파일:
- `data/{종목명}/financial_summary.json` -- 재무 요약 (손익/BS/CF/비율)
- `data/{종목명}/_per_band.json` -- 5Y PER/PBR 밴드 + z-score
- `data/{종목명}/_volatility_beta.json` -- Beta + σ_annual + R²
- `data/{종목명}/_peer_snapshot.json` -- Peer 실시간 시총/PER/PBR

**업종키 목록** (`peer_snapshot.py`): kpop, auto, semicon, battery, shipbuild, finance, pharma, retail, air, it, default. 해당 업종 없으면 `default` + `name:code` 개별 인자 제공.

**US 종목:**
```bash
python scripts/financial_summary_us.py {TICKER} [EXCD]
# FDR/베타/Peer 는 yfinance 내장으로 financial_summary_us.py 내에서 통합 처리
```

→ `data/{종목명 또는 TICKER}/financial_summary.json` 생성.

**이 파일들의 수치를 analysis.json에 그대로 사용한다.** 공식 데이터이며 EPS/BPS/ROE/부채비율을 이미 계산해서 제공한다. STEP 1.6/1.7/2.3은 이 단계에 통합되었으므로 별도 실행 불필요.

**KR 제공 데이터 (KIS 7개 엔드포인트 통합):**
- **KIS 시세**: 시총, PER, PBR, EPS, BPS, 52주 고저, 배당수익률, Forward PER(12M), 업종 PER, EPS 교차검증
- **손익계산서** (최대 22년): 매출, 매출원가, 매출총이익, 영업이익, 경상이익, 당기순이익 + 원가율/OPM/NPM 자동 계산
- **재무상태표** (최대 22년): 총자산/유동/비유동, 총부채, 자본총계, 이익잉여금
- **재무비율** (계산 불필요): ROE, ROA, 부채비율, 유동비율, 차입금의존도, 매출/영업이익/순이익/자기자본/총자산 증가율, EBITDA, EV/EBITDA, 배당성향, EVA
- **이상치 경고**: 매출 30%+ / 영업이익 50%+ 급변, OPM 음수, 부채비율 300%+ 등

**US 제공 데이터 (SEC EDGAR XBRL + KIS 해외주식 + yfinance):**
- **SEC XBRL**: 매출/원가/매출총이익/영업이익/순이익/EPS + 총자산/유동자산/현금/재고/부채/자본 + OCF/CapEx/FCF (5년 연간)
- **KIS 해외주식**: 현재가, 52주 고저 (PER/EPS가 0이면 yfinance fallback)
- **yfinance**: Forward PE/EPS, PEG, EV/EBITDA, 애널리스트 타겟/컨센서스, Beta, 공매도비율, 내부자/기관 보유비율
- **자동 파생**: OPM/NPM/ROE/ROA/부채비율/유동비율/FCF/Net Debt/Effective Tax Rate + YoY 성장률

**Q4 일회성 비용 체크 (한국 기업 특화):**
- 연간 영업이익/순이익에서 Q4 단독 비중 50%+ 또는 절댓값 집중 -> 일회성 여부 조사 필수
- 전형적: 유형자산 손상차손, 재고평가손실, 퇴직급여충당금, 특별상여, 대손충당금
- 일회성이면 s08에 **정상화 이익(Normalized Earnings)** 별도 계산하여 병기

---

## STEP 1.6: 변동성·베타 (STEP 1.5에 통합됨, 참고용)

**v4.7부터 STEP 1.5의 `volatility_beta.py`로 병렬 실행.** 아래 내용은 alg 참고 및 `data/{종목명}/_volatility_beta.json` 파일 읽기 방법만 남김.



```python
import json, numpy as np
# ⚠️ kis_api.get_daily_price()는 list[dict]를 반환하며 키는 **한글**:
#    '날짜' / '시가' / '고가' / '저가' / '종가' / '거래량'
stock_name = "JYP"  # 스킬 호출 시 확정된 종목명
d = json.load(open(f'data/{stock_name}/data_kis.json', encoding='utf-8'))
closes = [float(x['종가']) for x in d['daily_prices']][::-1]  # 시간순
rets = np.diff(np.log(closes))
vol_annual = float(np.std(rets) * np.sqrt(252))
# 정식 베타는 Q9(아래) 참조 — KOSPI200/KOSDAQ150 ETF 기준 회귀분석 필수
```

**결과 반영:**
- σ 40%+ → 고변동성, 포지션 2~3%
- σ 25~40% → 일반, 포지션 3~5%
- σ <25% → 저변동성, 포지션 5~7%
- 베타 1.5+ → 시장 하락 시 추가 손실 경고

---

## STEP 1.7: 5Y PER/PBR 밴드 (STEP 1.5에 통합됨, 참고용)

**v4.7부터 STEP 1.5의 `fdr_band.py`로 병렬 실행.** 결과는 `data/{종목명}/_per_band.json` 에 저장. z-score 밴드 테이블(s09 필수)과 밸류에이션 밴드 해석은 그대로 유지. 아래 상세 설명은 backup 참고용.



**⚠️ 절대 규칙: 연말 종가를 "대략"이라고 쓰거나 WebSearch 추정치를 쓰면 안 된다. 반드시 FinanceDataReader(FDR)로 정확 조회.**

에코프로/JYP 사건: 연말 종가 추정(예: 2021 "약 63,000원")이 실제값(50,700원)과 25% 어긋나 z-score가 잘못 계산되는 사고 발생. 재발 방지를 위해 FDR 필수.

**사전 설치 (최초 1회만):**
```bash
pip install finance-datareader
# ModuleNotFoundError 발생 시 위 명령 실행 후 재시도
```

```python
# KR 종목: FinanceDataReader로 정확한 연말 종가 일괄 조회
import FinanceDataReader as fdr
import json, numpy as np, sys
sys.path.insert(0, "scripts")
from kis_api import get_current_price

# ★ 1. 본 종목 식별 (스킬 호출 시 확정된 변수)
stock_name = "JYP"          # 예: "JYP", "기아", "현대차"
stock_code = "035900"        # 6자리 종목코드

# 2. KIS API로 현재 PER/PBR 조회 (z-score 계산용)
# ⚠️ kis_api.get_current_price()는 **한글 키**로 반환: 현재가/시가총액/PER/PBR/EPS/BPS/52주최고/52주최저
px = get_current_price(stock_code)
current_per = float(px.get("PER", 0))
current_pbr = float(px.get("PBR", 0))
current_price = int(px.get("현재가", 0))

# 3. FDR로 5년+ 일봉 가져와 연말 종가 추출
df = fdr.DataReader(stock_code, '2020-01-01', '2026-12-31')
year_end = df.groupby(df.index.year).tail(1)
closes = {int(idx.year): int(row['Close']) for idx, row in year_end.iterrows()}
# 예: {2020: 34350, 2021: 50700, 2022: 67800, 2023: 101300, 2024: 69900, 2025: 72600}

# 4. financial_summary에서 연도별 EPS/BPS 가져와 PER/PBR 시계열 계산
d = json.load(open(f'data/{stock_name}/financial_summary.json', encoding='utf-8'))
fin = d['financials']

per_series, pbr_series = [], []
for y in sorted(closes.keys()):
    year_fin = fin.get(str(y), {})
    eps = year_fin.get('eps')
    bps = year_fin.get('bps')
    if eps and eps > 0:        # 적자 연도(음수 EPS)는 제외
        per_series.append(closes[y] / eps)
    if bps and bps > 0:
        pbr_series.append(closes[y] / bps)

per_mean, per_std = float(np.mean(per_series)), float(np.std(per_series))
pbr_mean, pbr_std = float(np.mean(pbr_series)), float(np.std(pbr_series))

# 5. 현재 PER/PBR의 z-score
current_per_z = (current_per - per_mean) / per_std if per_std else 0.0
current_pbr_z = (current_pbr - pbr_mean) / pbr_std if pbr_std else 0.0

# 6. data/{종목명}/_per_band.json 으로 저장 (s09/s02에서 재사용)
json.dump({
    "closes": closes, "per_series": per_series, "pbr_series": pbr_series,
    "per_mean": per_mean, "per_std": per_std,
    "pbr_mean": pbr_mean, "pbr_std": pbr_std,
    "current_per": current_per, "current_per_z": current_per_z,
    "current_pbr": current_pbr, "current_pbr_z": current_pbr_z,
}, open(f'data/{stock_name}/_per_band.json', 'w', encoding='utf-8'),
   ensure_ascii=False, indent=2)

print(f"[OK] 5Y PER band: mean={per_mean:.2f}x, std={per_std:.2f}, current={current_per:.2f}x (z={current_per_z:+.2f}σ)")
print(f"[OK] 5Y PBR band: mean={pbr_mean:.2f}x, std={pbr_std:.2f}, current={current_pbr:.2f}x (z={current_pbr_z:+.2f}σ)")
```

**US 종목**: `fdr.DataReader('AAPL', '2020-01-01')` 또는 yfinance 사용. 동일 로직.

**밴드 테이블 형식 (s09 필수 삽입):**
```markdown
| 지표 | -1σ | 평균 | +1σ | 현재 | z-score | 백분위 |
|---|---|---|---|---|---|---|
| PER | 15.0x | 22.0x | 29.0x | 13.3x | -1.82σ | 3% (역사적 저점) |
| PBR | 1.8x | 2.5x | 3.2x | 3.2x | -1.62σ | 5% (저점권) |
```

- 백분위 ≥90% → "역사적 고점권" 경고 필수
- 백분위 ≤10% → "역사적 저점권" 명시 + 저평가 논거
- 현재가 +1σ 초과 → 리레이팅 근거 필수
- 모든 z-score는 **숫자 그대로** (반올림 한 자리까지) 표기. "약" 금지.

**절대 금지:**
- WebSearch "JYP 2021 연말 주가" 식의 간접 조회 — 정확하지 않음
- "약 63,000원" 같은 추정치 기입 — 25%+ 오차 위험
- EPS 적자 연도를 PER 평균에 포함 — 왜곡 발생 (음수 PER은 제외하고 계산)

---

## STEP 2: 데이터 검증

```python
# 1. KIS 현재가 / PER = EPS 교차검증
# 2. 확정 실적(2023~2025) vs Forward 추정(2026E~) 구분
#    - 확정 연도에 절대 추정치를 넣지 말 것
# 3. 사업부 매출 비중: DART 사업보고서 실제 수치 or "추정" 명시
```

---

## STEP 2.3: Peer 실시간 조회 (STEP 1.5에 통합됨, 참고용)

**v4.7부터 STEP 1.5의 `peer_snapshot.py`로 병렬 실행.** 결과는 `data/{종목명}/_peer_snapshot.json` 에 저장. Peer 테이블 작성 시 이 파일을 읽어서 1:1 대조. 아래 원본은 backup 참고용.



**⚠️ 절대 규칙: Peer 회사 시총/PER/PBR을 "대략" "추정"으로 쓰거나 웹 검색 수치를 그대로 쓰면 안 된다. 반드시 KIS API로 본 종목과 같은 시점에 실시간 조회.**

에코프로/JYP 사건: Peer 테이블에 HYBE "~17조/PER 25x"를 썼는데 실제는 10.95조/적자(-44.77). SM을 "2.3조/PER 20x"로 썼는데 실제는 1.96조/PER **5.66x** → "Big 4 최저 PER"이라는 핵심 논거가 통째로 거짓이 됨. 재발 방지를 위해 Peer도 반드시 KIS 실시간.

```python
# KR Peer: 같은 업종 상위 3~5개 종목을 선정 후 일괄 조회
import json, sys
sys.path.insert(0, "scripts")
from kis_api import get_current_price

# ★ 본 종목 (스킬 호출 시 확정된 변수 — STEP 1.7과 동일한 이름)
stock_name = "JYP"   # 예: "JYP", "기아", "현대차"

# 1. Peer 종목코드 리스트 작성 (업종 선도 3~5개)
#    - Q7 규칙: 같은 산업 + 시총 규모 ±3배 이내 + 사업 모델 유사
#    - 예: K-pop → [HYBE 352820, SM 041510, YG 122870, 큐브 182360]
#    - 예: 자동차 → [현대차 005380, 기아 000270, 현대모비스 012330]
#    - 예: 반도체 → [삼성전자 005930, SK하이닉스 000660, DB하이텍 000990]
peer_codes = {
    "HYBE": "352820",
    "SM Entertainment": "041510",
    "YG Entertainment": "122870",
}

# ★ 루프 변수는 stock_name과 충돌 금지 — peer_name 사용
# ⚠️ kis_api.get_current_price()는 **한글 키**로 반환한다:
#    현재가 / 시가총액(억) / PER / PBR / EPS / BPS / 52주최고 / 52주최저 / 등락률 / 거래량
#    소문자 per/pbr, stck_prpr, hts_avls는 KIS 원시 API 필드명이며 kis_api.py 래퍼는 사용하지 않는다.
peer_snapshot = {}
for peer_name, peer_code in peer_codes.items():
    px = get_current_price(peer_code)
    peer_snapshot[peer_name] = {
        "code": peer_code,
        "price": int(px.get("현재가", 0)),
        "market_cap_uk": int(px.get("시가총액", 0)),  # 억 단위 (예: 21391)
        "per": float(px.get("PER", 0)),               # 적자면 음수 그대로 유지
        "pbr": float(px.get("PBR", 0)),
        "eps": float(px.get("EPS", 0)),
        "bps": float(px.get("BPS", 0)),
        "high_52w": int(px.get("52주최고", 0)),
        "low_52w": int(px.get("52주최저", 0)),
    }

# 저장 (s05/s09에서 재사용) — 반드시 stock_name 사용 (루프 변수 아님)
json.dump(peer_snapshot,
          open(f'data/{stock_name}/_peer_snapshot.json', 'w', encoding='utf-8'),
          ensure_ascii=False, indent=2)

# 검증: 본 종목 대비 Peer 순위 확인
# "Big 4 최저/최고 PER" 같은 표현을 쓰려면 이 스냅샷과 **반드시** 일치해야 함
for pn, d in peer_snapshot.items():
    print(f"  {pn}: mcap={d['market_cap_uk']:,}억 PER={d['per']} PBR={d['pbr']}")
```

**⚠️ 변수 이름 주의:**
- `stock_name` = 본 종목 (예: "JYP") — 파일 경로에 사용
- `peer_name` = 루프 변수 (Peer 이름) — 절대 `name`으로 쓰지 말 것
- 과거 사고: 루프 변수를 `name`으로 쓰면 외부 종목명 변수와 충돌하여 `data/YG Entertainment/_peer_snapshot.json`에 저장되는 버그 발생

**US Peer**: `get_us_current_price(ticker, excd)` 사용. 동일 로직.

**Peer 테이블에 반드시 반영:**
```markdown
| 회사 | 시총 | PER | PBR | 비고 |
|---|---|---|---|---|
| 본 종목 (하이라이트) | 2.14조 | 13.32x | 3.21x | 5Y -1.82σ |
| HYBE | 10.95조 | **적자 (-44.77)** | 3.31x | 하이브리드 유니버스 리셋 |
| SM | 1.96조 | **5.66x** | 1.95x | Big 4 최저 — 구주매출 일회성 왜곡 |
| YG | 0.96조 | 25.94x | 1.85x | 2NE1/블핑 컴백 의존 |
```

**절대 금지:**
- "약 X조", "PER 약 20x" 같은 추정치 (실시간 데이터가 있는데 왜 추정?)
- "Big 4 최저 PER" 같은 주장을 Peer 스냅샷 대조 없이 쓰기
- Peer의 적자 상태를 "PER 20x+"처럼 양수로 날조

**Peer 시총/PER/PBR에 대한 주장 = _peer_snapshot.json 원본과 1:1 일치해야 함. STEP 6에서 강제 점검.**

---

## STEP 2.5: 사업보고서 정독 (v4의 핵심 — 이 단계가 리포트 품질을 결정)

**⚠️ 이 단계를 대충 하면 s03/s04/s02가 얕아진다. v3에서 품질이 기대만큼 안 나온 근본 원인이 여기였다.**

먼저 자동 추출 도구를 돌린다:
```bash
python scripts/report_extractor.py data/{종목명}/data_dart_reports.json
# → data/{종목명}/report_quotes.json 생성
```

### 📚 3층 읽기 전략 (v4.10 재설계 -- 분기보고서 SKIP 규칙 폐기)

`data_dart_reports.json`에는 **최대 7개 보고서**가 포함된다(v4.10 한도 4→6+1 확대).
**SKIP 금지. 최신이 가장 중요하다** -- 분기보고서에도 신규 계약·소송·CapEx 변경이 공시된다.

```
[0] 최신 사업보고서 (연간 결산)    🟢 PRIMARY DEEP — 10 Grep 패턴 전체 적용
[1] 최신 분기/반기 보고서           🟠 LATEST CHANGES — 변화점 집중 (신규 공시만)
[2] 그 다음 분기/반기              🟠 LATEST CHANGES — 전전 분기와 Diff
[3] 그 이전 분기/반기              🟡 TREND — 최근 4개 공시 흐름 파악
[4] 전년 사업보고서                🔵 YoY DIFF — 지역별 M/S / 판매대수 / 위험요인 Diff
[5~6] 추가 과거 정기보고서         🔵 OPTIONAL — 5년 추세 필요 시
```

**핵심 원칙**:
- **최신 공시일 순서 존중** — 오늘 시점이 2026/04이면 가장 최근은 **2026/03 발표 사업보고서** (2025/12 결산). 그 다음 최신은 **2025/11 발표 3분기보고서** (2025/09 결산).
- **분기보고서의 가치**: (a) 최근 3~6개월 신규 계약/수주/투자/JV 공시, (b) "위험 요인" 섹션의 최근 업데이트(전 분기 대비 새로 추가된 리스크 키워드), (c) 경영진 교체/임원 보수 변화, (d) 원재료 가격/환율 민감도 최신 수치
- **읽기 시간 배분**: [0] 50% + [1][2] 30% + [3][4] 15% + [5][6] 5% — 최신에 시간 집중
- **"전년 대비 변화"는 [4]와 비교, "전 분기 대비 변화"는 [1]~[3]끼리 비교** — 두 Diff가 다른 시그널 포착

**⚠️ 분기보고서 부재 종목 처리 (삼성전자·SK하이닉스 등 시총 상위 대기업)**:
일부 대기업은 DART에 **반기+사업보고서만 제출하고 1·3분기 보고서는 공시하지 않는다** (IR 자료·실적발표자료로 대체). `data_dart_reports.json`에서 분기보고서가 없으면:
1. **분기 재무**: `data_kis_financials.json`의 분기 손익 데이터로 대체
2. **분기 어닝 서프라이즈**: `_wisereport.json.earning_surprise`가 연간·분기 컨센 대비 실적 제공
3. **분기 뉴스**: WebSearch "{종목명} {현재년도}/Q1 실적 발표 주요 공시" 추가 쿼리로 보완
4. **s15 Beat/Miss에 명시**: "DART 분기보고서 부재 종목이므로 분기 상세는 IR 자료(회사 홈페이지) + Wisereport 추정기관 데이터 기반" 주석 추가

### 🎯 필수 Grep 패턴 (s03/s04의 90% 데이터가 여기서 나온다)

먼저 각 보고서의 **sections 키**를 파악한다:
```python
import json
d = json.load(open('data/{종목명}/data_dart_reports.json', encoding='utf-8'))
for i, r in enumerate(d[:3]):
    print(f"[{i}] {r.get('report_nm','?')} sections:", list(r.get('sections',{}).keys()))
```

가장 중요한 3개 섹션을 Read (혹은 Grep) — 각각 파일로 덤프해서 Grep:

```python
# v4.10: 최신 3~4개 보고서 (사업+최근 분기/반기) + 전년 사업보고서 덤프
import json
d = json.load(open('data/{종목명}/data_dart_reports.json', encoding='utf-8'))

# 덤프 대상: 최신 3개 (일반적으로 사업+최근 분기+직전 분기) + YoY 비교용 전년 사업보고서
dump_targets = [0, 1, 2]  # 최신 3개 모두
# 전년 사업보고서 찾기 (type="사업보고서" 중 [0]을 제외한 최초)
for i in range(1, len(d)):
    if d[i].get('type') == '사업보고서' and i not in dump_targets:
        dump_targets.append(i)
        break

for idx in dump_targets:
    if idx >= len(d): break
    r = d[idx]
    for sec_name, short in [('사업의_내용','biz'), ('주요_계약','contract'),
                            ('위험_요인','risk'), ('연구개발','rnd')]:
        body = r.get('sections', {}).get(sec_name, '')
        if not body: continue
        # ⚠️ 프로젝트 루트 오염 금지 — 반드시 data/{종목명}/ 하위에 저장
        out_path = f'data/{stock_name}/_tmp_r{idx}_{short}.txt'
        with open(out_path, 'w', encoding='utf-8') as f:
            f.write(body)
    print(f"[{idx}] {r.get('type','?')} {r.get('report_nm','')} 덤프 완료")
```

**중요**: `_tmp_*.txt`는 반드시 `data/{종목명}/` 하위에 저장해야 한다. 프로젝트 루트에 저장하면 다음 /research 실행 시 **다른 종목의 덤프 파일이 섞여** s03/s04가 틀린 회사 데이터로 작성되는 사고가 발생할 수 있다.

**Grep 시에도 경로 지정 필수:**
```bash
# O 올바름 — 종목 폴더 내에서만 검색
Grep pattern="지역별" path="data/{종목명}" glob="_tmp_*.txt"

# X 잘못 — 프로젝트 루트에서 검색 → 이전 종목 잔재 섞임
Grep pattern="지역별" glob="_tmp_*.txt"
```

### ⚡ Grep 병렬 호출 (v4.7 최적화 -- 순차 5분 → 병렬 2분)

**⚠️ 핵심**: 아래 10개 Grep 패턴은 **반드시 한 메시지 안에 동시에 호출**한다. 순차 호출은 5분+ 낭비. Claude Code의 병렬 tool call 기능으로 한번에 10개 Grep을 던지면 2분 내 완료.

```
# 단일 메시지에 10개 Grep 호출 (병렬 실행 자동)
Grep(pattern="지역별", path="data/{종목명}", glob="_tmp_*.txt")
Grep(pattern="시장점유율|M/S", path="data/{종목명}", glob="_tmp_*.txt")
Grep(pattern="판매.*대|만대", path="data/{종목명}", glob="_tmp_*.txt")
Grep(pattern="ASP|평균판매단가", path="data/{종목명}", glob="_tmp_*.txt")
Grep(pattern="친환경차|하이브리드|HEV", path="data/{종목명}", glob="_tmp_*.txt")
Grep(pattern="RV|SUV", path="data/{종목명}", glob="_tmp_*.txt")
Grep(pattern="신용평가|Moody|S&P", path="data/{종목명}", glob="_tmp_*.txt")
Grep(pattern="특허|지적재산권", path="data/{종목명}", glob="_tmp_*.txt")
Grep(pattern="정관|신규 목적사업", path="data/{종목명}", glob="_tmp_*.txt")
Grep(pattern="계약|수주|JV", path="data/{종목명}", glob="_tmp_*.txt")
```

### 🔍 10개 필수 Grep 패턴 (s03 데이터 채굴)

아래를 **반드시** Grep하여 발견한 내용은 s03에 전부 반영:

| # | 찾을 것 | Grep 패턴 | 어느 섹션에 반영 |
|---|---|---|---|
| 1 | **지역별 매출 테이블** | `지역별`, `국내`, `북중미`, `유럽`, `(단위.*백만원)` | s03 테이블 |
| 2 | **시장 점유율** | `시장 점유율`, `시장점유율`, `M/S`, `점유율` | s03 + s04 |
| 3 | **판매대수 YoY** | `판매.*대`, `만대`, `전년.*증가`, `전년.*감소` | s03 테이블 |
| 4 | **ASP (평균판매단가)** | `ASP`, `평균판매단가`, `백만원.*기록` | s03 프리미엄화 |
| 5 | **친환경차/HEV 비중** | `친환경차`, `하이브리드`, `HEV`, `EV.*비중` | s03 + s04 |
| 6 | **RV/SUV 비중** | `RV`, `SUV`, `판매 비중` | s03 |
| 7 | **신용등급 이력** | `신용평가`, `Moody`, `S&P`, `AAA`, `AA\+`, `A-` | s03 재무 요새 |
| 8 | **지적재산권** | `특허`, `디자인`, `지적재산권` | s03 해자 |
| 9 | **신규 목적사업/정관 변경** | `정관`, `신규`, `추가`, `목적사업` | s03 변화 시그널 |
| 10 | **주요 계약/수주** | `계약`, `공급`, `수주`, `JV`, `합작` | s11 카탈리스트 |

### 🎯 [0] vs [2] Diff — 전년 대비 변화 발굴

**전년 보고서를 처음부터 읽지 말 것.** 아래 3개 질문만 Grep으로 답:

1. **"전년엔 없던 새 위험 키워드"** — 관세/원재료/환율/규제 키워드 추가 확인
2. **"전년엔 없던 새 전략/계약"** — 신규 투자, JV, 신규 사업 부문
3. **"전년엔 강조된 것이 올해 사라짐"** — 축소 시그널

발견한 변화점 **최소 3개**를 s03의 "전년 대비 변화 시그널" 블록에 정리.

### 📌 회사별 맞춤 Grep (업종 따라 추가)

- **제조업**: `생산능력`, `공장`, `가동률`, `CapEx`
- **자동차**: `판매 지역`, `수출`, `부품업체`, `EV`, `배터리`
- **바이오**: `임상`, `파이프라인`, `FDA`, `특허 만료`
- **IT/플랫폼**: `MAU`, `DAU`, `ARPU`, `매출 구조`
- **지주회사**: `자회사`, `지분`, `NAV`
- **리츠/부동산**: `NOI`, `공실률`, `자산 가치`

**이 Grep 10개를 건너뛰면 s03/s04는 절대 기아 리포트 수준이 나오지 않는다.**

---

## STEP 3: 심층 분석 — "So What?" (리포트 가치의 핵심 단계)

**데이터는 충분하다. 이제는 "생각"할 차례다.** 속도에 쫓기지 말 것.

### Phase 1: 이해 — "이 회사는 진짜 뭘로 돈 버는 회사인가?"

사업보고서 + 재무제표 + 웹 검색 + STEP 2.5 Grep 결과를 종합하여 아래에 **자기 말로** 답한다:

```
1. 이 회사의 돈 버는 구조
   → 한 문장: "X를 만들어서 Y에게 파는 회사"
   → 세그먼트별 영업이익을 보면, 실제로 돈을 버는 사업은 어디?
   → (단일 세그먼트면 그렇게 명시 + "마진 드라이버는 어디?")

2. 재무제표에서 "가장 이상한 숫자" 3개 + "왜?" 3단계
   → ①: (숫자) — 1단계 표면 → 2단계 원인 → 3단계 근본
   → ②: ...
   → ③: ...

3. 지역별/제품별 성장 패턴
   → STEP 2.5 Grep에서 찾은 지역별 M/S·판매대수 YoY로 판단
   → "어느 지역이 성장 중이고 어느 지역이 둔화 중인가?"

4. 모르는 것 5가지 → 추가 WebSearch
```

### Phase 2: 판단 — "시장이 틀리고 있는 것은 무엇인가?"

```
1. 현재 주가가 반영하는 시나리오
   → PER에서 역산: 시장이 기대하는 EPS 성장률?
   → 낙관적? 비관적? 왜?

2. "내가 시장보다 더 잘 아는 것" (Edge)
   → 사업보고서 [0] Grep에서 찾은 구체적 사실
   → 컨센서스에 미반영된 것
   → 없으면: 이 종목에 edge 없음 → HOLD 권고

3. "이 주식을 사면 안 되는 이유" 3가지 + 데이터 기반 반박
   → 반박이 "희망"이 아닌 "숫자"로 됐는가?
   → 2개 이상 강한 반박 = BUY 가능
   → 미만 = HOLD/SELL

4. "주가를 반토막 낼 수 있는 가장 큰 위험" 1가지
```

### Phase 3: 확신 — "3년 후 이 회사는 어떻게?"

```
1. Bear / Base / Bull 3시나리오 구체 숫자
   → 각 시나리오의 매출/OP/EPS
   → 확률 주관 배정

2. DCF 계산
   → Q1 가정 테이블 (WACC/g/Beta/Rf/ERP/Kd)
   → Q2 민감도 테이블 (매출 CAGR × OPM)
   → Q3 역사 밸류 밴드
   → 적정주가 → 안전마진

3. R/R (Risk-Reward)
   → 하방(Bear까지) vs 상방(Bull까지)
   → 1:2 이상 → BUY 가능
   → 미만 → HOLD

4. 투자 논문 한 문장
   → "나는 [이유] 때문에 [종목]을 [BUY/HOLD/SELL]한다"
```

---

## STEP 3.5: 분석 메모 출력 (v4.7부터 STEP 3 마지막에 통합 실행)

**v4.7 최적화**: STEP 3 Phase 3 완료 직후 아래 메모를 **연속으로** 출력한다. 별도 단계 전환 없이 같은 메시지에서 작성하여 LLM 컨텍스트 전환 오버헤드 제거.



**Phase 1/2/3 결과를 사용자에게 텍스트로 보여준다.** 머릿속에서만 생각하면 대충 넘어가므로, 글로 강제한다.

```
=== 분석 메모: {종목명} ===

[1] 이 회사는 뭘로 돈 버나?
→ (한 문장)
→ 실제 마진 드라이버: (어디서 돈 버는지)
→ 핵심 경쟁 우위: (왜 경쟁사보다 잘 버나?)

[2] 재무에서 가장 이상한 숫자 3개
→ ①: 숫자 — 왜? → 왜? → 왜?
→ ②: ...
→ ③: ...

[3] 내가 시장보다 더 잘 아는 것 (Edge)
→ (구체적 인사이트 1~2개, 사업보고서 [0] 직접 인용)

[4] 사면 안 되는 이유 3가지 + 반박
→ #1: (이유) — 반박: (데이터)
→ #2: ...
→ #3: ...
→ 반박 강도: 3개 중 __개 강함 → BUY/HOLD/SELL

[5] 투자 논문 한 문장
→ "나는 ___ 때문에 ___을 ___한다"

[6] 숫자
→ DCF 적정주가: ___원 (안전마진 __%)
→ R/R: 하방 __% vs 상방 __% = 1:__
→ Bear/Base/Bull: ___/___/___원
```

**이 메모를 출력한 후에만 STEP 4(JSON 작성)로 넘어간다.**

---

## STEP 4: analysis.json 작성

**섹션 작성 순서 (중요):**

먼저 **s02/s03/s04**를 작성한다. 이 3개가 가장 약한 부분이었고, 가장 공들여야 한다. 그 다음 s08(재무) → s09(밸류) → 나머지 섹션.

참고 파일: `scripts/analysis_template.json`, `scripts/analysis_NFLX.json`

---

### 🔥 본문 서술 품질 6대 원칙 (모든 섹션에 무조건 적용 -- v4.4 신설)

**v4 이전 리포트는 "불릿 나열 + 얕은 숫자"가 고질병이었다. v4.4부터 아래 6개 원칙을 강제한다. 위반 시 재작성.**

#### 원칙 1: 불릿 남용 금지 -- 기본은 "문단 서술"
- **한 섹션당 불릿(-, * 시작 행) 수는 전체 줄의 30% 이하**
- 데이터 테이블(|) 이나 공식 박스가 아닌 곳에는 가급적 문단 서술
- **❌ 나쁜 예**: "- 매출 25.23조 YoY +41.2% / - OPM 4.4% -7.4%p / - 순이익 6,473억 -53%"
- **✅ 좋은 예**: "2025년 매출은 25.23조로 전년 대비 +41.2% 증가했는데, 이는 아시아나 연결 편입 효과가 반영된 결과다. 다만 영업이익률은 11.8%에서 4.4%로 7.4%p 급락했고 순이익도 6,473억으로 반토막 났다. 이 괴리의 원인을 파고들면 감가상각비가..." (문단 서술 + 왜? 연결)

#### 원칙 2: 모든 주장에 "3요소" 강제 -- 주장 / 근거 숫자 / 시사점
- 한 문단 안에 반드시 세 요소가 존재해야 한다
  - **주장(What)**: 한 줄 명제 ("OPM 급락은 회계 왜곡이다")
  - **근거(Why·숫자)**: 구체 수치 + 출처 ("감가상각이 1.74→2.74조, 1조 증가. financial_summary.json")
  - **시사점(So What)**: 투자 판단에 어떤 영향? ("→ EPS 2,020원은 저평가 시그널이며 2027년 정상화 시 3,450원 복귀")
- **3요소가 빠진 문단은 재작성**

#### 원칙 3: 위닝펀드식 "소제목 + 3~5문단" 구조
- 각 섹션의 하위 주제는 반드시 **굵은 소제목(###) + 3~5문단 서술**
- 소제목은 **단순 라벨이 아닌 메시지**여야 한다
  - ❌ "재무분석" (단순 라벨)
  - ✅ "2025년 -47%의 진실 -- 감가상각이 만든 가짜 하락" (메시지 담은 소제목)

#### 원칙 4: 숫자는 반드시 "출처 + 추이 + 해석" 세트로
- **❌**: "부채비율 340%"
- **✅**: "부채비율 340%(financial_summary.json). 2023년 210% → 2024년 329% → 2025년 340%로 2년간 +130%p 급등했는데, 이는 아시아나 B/S 연결 편입 효과가 절대적이다. 3대 신용평가사는 같은 기간 오히려 A등급으로 상향했다."

#### 원칙 5: 전문용어는 반드시 괄호 내 풀어쓰기 (한 번이면 충분)
- **❌**: "EV/EBITDA 5.69x는 Peer 대비 +21%"
- **✅**: "EV/EBITDA(기업가치를 연간 현금창출력으로 나눈 배수) 5.69x는 Peer 대비 +21%"
- 첫 등장 시만 풀어쓰고, 이후는 약어 사용 OK
- 해당 용어: OPM, EBITDA, ROE, ROIC, PMI, EV/EBITDA, PBR, PSR, WACC, DCF, SOTP, NAV, FCF 등

#### 원칙 6: 서술 톤은 "단정 + 확신" -- "~것으로 보인다/추정된다" 금지
- **❌**: "~것으로 보인다", "~것으로 추정된다", "~할 수도 있다"
- **✅**: "~다", "~이다", "~한다"
- 확신 없는 주장은 애초에 쓰지 말 것. 쓰면 단정적으로.
- 예외: 확률/시나리오 논의는 "70% 확률로 ~한다" 식으로 구체화

---

### 📌 meta 필수 필드 (v4.4 추가)

analysis.json 작성 시 meta 객체에 **`tagline` 필드를 필수 추가**한다 (커버 페이지 subtitle 용):

```json
"meta": {
  "stock_name": "...",
  "stock_code": "...",
  ...
  "tagline": "방산을 품은 하늘길 -- 저평가는 착시인가"
}
```

**tagline 작성 규칙**:
- **한국어 10~20자**
- **종목 특성 + 투자 메시지를 위트/은유/도발적 표현으로 압축**
- 위닝펀드 참고 예시:
  - "P의 거짓, 그 P 아닙니다" (네오위즈)
  - "나 혼자만 고인물 만렙 뉴비" (휠라홀딩스)
  - "비침습 시술의 FIRST CLASS" (클래시스)
  - "HIgh, My Name Is SK HIGHnix" (SK하이닉스)
  - "써글놈, 지금 많이 사둬야 한다" (Circle)
- **❌ 금지**: "Balance of risk tilts to the downside" (영문 기계적), "{산업}의 비대칭 기회" (추상 클리셰)
- 종목별 고유 맥락(제품명, 기술명, 산업 모멘텀) 반드시 반영

### 🎓 v4.9 신설 -- 위닝펀드 Few-shot 예시 + 섹션별 첫 문단 템플릿 강제

**지시 강화만으로는 서술 질이 60점대에서 멈춘다**. v4.9부터 위닝펀드 실제 우수 리포트 서술 패턴을 **복제 가능한 예시** 로 제공하고, 각 섹션 **첫 문단을 템플릿 빈칸 채우기** 방식으로 강제한다.

#### 📘 위닝펀드 실제 서술 예시 (이 패턴을 그대로 복제)

**예시 A -- 클래시스 ROCE 3년 추이 + Peer 비교 + 해석** (s08 재무분석 모범 사례):
> "동사의 경우 2021~2023년까지 25~28% 수준의 높은 ROCE(투하자본수익률)를 기록하여 자본 대비 수익성이 매우 우수하였고, 2024년에는 21.05%로 다소 하락했으나 여전히 20% 이상의 양호한 수준을 기록하고 있다. Peer 기업인 휴젤과 비교했을 때 동사의 평균 ROCE(26.065%)는 휴젤의 평균 ROCE 대비 2배 이상의 수준을 꾸준히 보이고 있어 동일 산업군 대비 뛰어난 수익성을 기록하고 있다고 볼 수 있다."

→ **패턴 해부**: [3년 수치 제시 25~28%] → [2024년 변화 21.05%] → [Peer 평균 26.065% 비교] → [의미 해석 "2배 이상 수준"] → [결론 "뛰어난 수익성"]. **숫자 4개 + 해석 + 결론 = 3요소 완비**.

**예시 B -- 클래시스 매각 리스크 + 밸류에이션** (s09 밸류에이션 모범 사례):
> "이렇게 계산한 평균 PER에 뷰티 산업의 흥행과 향후 수출 확대 기대감을 반영하여 20%~30%의 프리미엄을 적용하려고 했으나, 현재 클래시스가 베인 캐피탈의 매각 유치 과정에서 블록딜이 다시 발생할 수 있는 리스크가 존재하여 프리미엄을 반영하지 않고 보수적으로 PER를 산정했다."

→ **패턴 해부**: [가정 A (프리미엄 20~30%)] → [반대 근거 (매각 리스크)] → [결정 (보수 채택)]. **Why 검토 → 일부 철회 → 최종** 3단 논증.

**예시 C -- 네오위즈 Peer PER 방식** (s09 Peer 선정 모범 사례):
> "동사는 자체 IP와 퍼블리싱 역량을 보유한 중견게임사로, 수익구조와 성장전략 측면에서 크래프톤, 펄어비스, 시프트업 등과 유사한 포지셔닝을 점하고 있다. 이들 기업은 모두 일정 수준 이상의 글로벌 매출 비중을 확보하고 있으며, 콘솔·PC·모바일을 아우르는 멀티플랫폼 게임서비스 구조를 갖추고 있다는 공통점을 지닌다."

→ **패턴 해부**: [본 종목 정체성] → [Peer 3개 나열] → [선정 근거 2가지 (매출 + 플랫폼)]. **왜 이 Peer인지 서술**.

#### 🏆 2025 베스트 애널리스트 실제 서술 예시 (프로 톤 -- 기관용)

위닝펀드(대학 동아리)만 참고하면 casual 톤으로 흐른다. **기관 투자자가 실제로 읽는 프로 애널리스트의 단정·수치 중심 서술**을 함께 복제한다. 아래는 2025 하반기 베스트 애널리스트 상위권의 실제 인용 표현이다.

**예시 D -- 최정욱 (하나증권 은행, 2025 베스트 애널리스트 은행 부문)** -- s09 밸류에이션 / s01 투자의견 톤:
> "은행업종에 대한 투자의견 비중확대(Overweight)를 유지하고, 모든 은행지주사의 목표주가를 일제히 상향했다. 2025년 실적 공시가 마무리되며 주당순자산(BPS) 기준 연도를 2026년으로 변경했고, 이를 반영해 목표 주가순자산비율(PBR)을 기존 0.85배에서 0.9배로 높였다. 지난해 4분기 이후 은행주 상승률은 35.6%에 그쳐 코스피 상승률 54.8%를 여전히 하회한다. 은행주 평균 PBR은 0.69배에 불과해 은행주 랠리 현상은 계속 진행될 공산이 크다. 최선호주로 리딩뱅크 프리미엄이 부각될 KB금융과 대형 은행지주 내 상대적 저평가 매력이 부각될 하나금융을 제시한다."

→ **패턴 해부**: [투자의견 한 줄 단정] → [목표가 상향 수치 0.85배 → 0.9배] → [상대 벤치마크 35.6% vs 54.8%] → [절대 수치 PBR 0.69배 + "불과해"라는 단정] → [**결론: 최선호주 2개 + 각각의 선정 근거 한 줄**]. **"~것으로 보인다" 제로. "단정 + 수치" 100%**. 첫 5줄 안에 수치 6개.

**예시 E -- 최정욱 (하나증권 은행) 리스크 문단** -- s07 리스크 / s14 약세 논리 톤:
> "금리 상승 사이클 마무리로 NIM(순이자마진) 추가 개선은 어렵다. 대손충당금은 2조 원 이상 증가가 필요하고, 부동산 PF 유동성 문제와 가계 신용대출 연체율 상승이 동시에 진행되고 있다. 금리가 오르고 경기가 꺾이면 후행적으로 조달 비용이 커지면서 이익 개선 폭이 낮아진다."

→ **패턴 해부**: [NIM 개선 한계] → [충당금 2조+ 구체 수치] → [PF·연체율 2개 리스크 나열] → [**메커니즘 설명: "금리↑ + 경기↓ → 조달비용↑ → 이익개선폭↓"**]. **리스크도 메커니즘으로 설명**, 단순 나열 금지.

**예시 F -- 김선우 (메리츠 반도체, SK하이닉스 베스트 애널리스트) 산업 전망 톤**:
> "업사이클 내 차별화된 선도력이 중요하다. HBM 이후에도 리더십을 유지해야 재평가가 지속될 수 있으며, AI 기술 발전의 속도와 변화에 주목해야 한다."

→ **패턴 해부**: [업사이클 진단] → [핵심 변수 한 줄 "리더십 유지"] → [**조건부 프레이밍: "~해야 재평가가 지속"**]. 조건부 프레이밍은 상승 논거에 **검증 체크리스트**를 부여한다.

**예시 G -- 최정욱 실제 원문 (하나증권, 2025.12.01 은행 Weekly)** -- s01 투자의견 / s06 수급 톤:
> "전주 은행주는 2.4% 상승해 KOSPI 상승률 1.9%를 소폭 초과상승. 10월의 KOSPI 대비 -20%p 초과급락세에서 벗어나 11월부터는 양호한 주가 흐름이 이어지고 있으며 11월 한달간은 주가가 약 5.9% 상승해 KOSPI 대비 10%p 가량 초과상승 중. 지난주에도 외국인은 KOSPI를 2.1조원 넘게 순매도했으며 은행주도 전전주에 이어 전주에도 3,000억원 넘게 순매도. 반면 국내 기관은 지난주 KOSPI를 3.4조원 이상 순매수했으며 은행주도 3,340억원을 순매수. 국내 기관들의 은행주 순매수 강도가 계속 강해지고 있는 양상."

→ **패턴 해부**: [한 줄 결론 "2.4% vs 1.9%"] → [과거 대비 맥락 "-20%p → +10%p"] → [**외국인 매도 vs 기관 매수를 수치로 대비** (2.1조 vs 3.4조, 3,000억 vs 3,340억)] → [해석 "순매수 강도 강해지는 양상"]. **수급 분석은 "누가 팔고 누가 샀는가"를 숫자 4~6개로 대비시킨다**.

**예시 H -- 최정욱 실제 원문 (과징금 분석)** -- s07 리스크 / s11 카탈리스트 "수치 시나리오화":
> "우리 추정에 따르면 은행별로는 KB금융은 약 0.95~1.0조원, 신한지주와 하나금융은 약 0.25~0.3조원 내외일 것으로 예상. 다만 금감원 사전통보 규모는 감경 기준이 반영되기 이전의 금액으로 12월 18일 예정된 제재심에서 과징금 감경사유가 적용되고 나면 실제 금액은 크게 낮아질 가능성이 높다고 판단. 개정된 금소법에 따르면 경미한 위법행위의 경우 1/2 범위내에서 과징금 조정이 가능하고, 내부통제 및 재발방지 노력 등에 따라 최대 75%까지도 추가 조정 가능. 따라서 실제 부과되는 과징금/과태료 규모는 1조원을 상당폭 하회할 여지도 높다고 판단."

→ **패턴 해부**: [자체 추정 범위 "KB 0.95~1.0조, 하나/신한 0.25~0.3조"] → [감경 조건 **1/2 범위 + 최대 75%**] → [결론 "1조 하회 여지 높다"]. **리스크 수치는 "최대치" + "감경 메커니즘" + "최종 예상치"를 3단으로**. "걱정된다"로 끝내지 않는다.

**예시 I -- 임소정 실제 원문 (유진투자, 2026.01.30 삼성전자 4Q25 Review)** -- s02 투자포인트 / s08 재무분석 "위트+숫자+결론":
> "4Q25 Review: 너무 잘나가서 죄송합니다. 4분기 부문별 영업이익(4Q24 → 4Q25)은 반도체 2.9조 → 16.4조, SDC 0.9조 → 2.0조, MX/NW 2.1조 → 1.9조, VD/DA 0.2조 → -0.6조, 하만 0.4조 → 0.3조로 전체 영업이익은 큰 폭 개선. VD/DA부문의 적자가 지속된 가운데 모바일 제조원가 상승으로 전년 및 전분기 대비 영업이익 감소. 한편 반도체 부문은 유리한 수급 환경에 따라 DRAM과 NAND 모두 수익성이 대폭 증가. 실적 추정치 상향에 따라 목표주가 210,000원으로 상향(Target P/B 2.5배) 및 투자의견 'BUY' 유지."

→ **패턴 해부**: [위트 있는 소제목 "너무 잘나가서 죄송합니다"] → [**5개 사업부 Before → After 대비**] → [누가 좋고 누가 나쁜지 한 줄씩] → [**목표가 + 밸류 배수 + 투자의견**을 한 문장으로 종결]. 커버 문단에서 독자는 **반도체 +13.5조 점프**를 바로 읽고, 다른 부문 부진은 그 다음 줄에서 균형 있게 파악.

**예시 J -- 박상욱 실제 원문 (신영증권, 2025.09.22 반도체 HBF 이니시에이션)** -- s04 산업분석 "메커니즘 기반 산업 논제":
> "당사는 AI 투자 지속에 따른 고용량 낸드 수요 증가와 관련된 업체들의 수혜를 전망한다. AI 대규모 언어 모델(LLM)의 매개변수가 급증하면서, 기존 HBM의 용량 한계가 드러나고 있다. LLM은 이전 대화 맥락을 빠르게 추론하기 위해 충분한 KV(Key-Value) 저장 용량을 필요로 하는데, 디램 기반의 HBM만으로는 이 요구를 충족하기 어렵다. 이에 따라 대용량과 고대역폭을 동시에 제공하는 새로운 메모리 솔루션인 HBF(High Bandwidth Flash)의 중요성이 부각되기 시작했다. HBF가 상용화되면 낸드 산업에 구조적 변화를 가져와 업황 개선 및 수익성 제고에 기여할 것으로 예상된다."

→ **패턴 해부**: [투자 명제 한 줄] → [**구조적 변수**(LLM 매개변수 증가)] → [**병목**(HBM 용량 한계)] → [**수요 메커니즘**(KV 저장 필요)] → [**해결책**(HBF)] → [산업 임팩트(구조적 변화 + 수익성 제고)]. **산업 리포트는 "트렌드 → 병목 → 해결책 → 수혜 기업" 5단 메커니즘**. 클리셰 "AI가 대세"가 아니라 **"왜 낸드인가"를 기술적 논리로 설명**.

**예시 K -- 양일우 실제 원문 (삼성증권, 2026.04 한국 주식시장 전망)** -- s04/s09 "밴드 + 시나리오 + 적용 배수":
> "4월 한국 주식시장 핵심 변수로서 1) 고유가 장기화 여부, 2) 이익 모멘텀 지속 여부, 3) 내국인 수급 모멘텀 변화 가능성 제시. 4월 월간 KOSPI 밴드를 4,700~5,900pt로 제시. 밴드 하단은 고유가 장기화 시나리오를 시장이 단기적으로 받아들일 가능성을 상정하였고, 상단은 최근 20년 평균 12개월 예상 P/E 10.3배를 10% 할인하여 9.3배를 적용하였음."

→ **패턴 해부**: [핵심 변수 **숫자 매긴 3개**] → [**구체 밴드 제시**(4,700~5,900)] → [**하단 근거**(시나리오)] → [**상단 근거**(20년 P/E 평균 × 할인율)]. **"목표가 X만원" 한 줄 대신 "밴드 + 각 끝점의 근거"를 제시하면 독자는 본인 위치를 파악 가능**. 이 패턴을 s01·s09에 적용 시 "Bull/Base/Bear"는 **각 끝점의 산정 근거 한 줄씩 병기** 필수.

#### 🧠 예시 G~K가 추가로 가르쳐 주는 것

1. **수급 분석 = 숫자 대비** (예시 G). "외국인 매도" 한 줄 대신 외국인 2.1조 매도 vs 기관 3.4조 매수를 같은 문장에서 병치.
2. **리스크 = 3단 메커니즘** (예시 H). 최대치 → 감경 경로 → 최종 예상치. "우려된다"로 끝내지 말 것.
3. **커버 문단 밀도 = 사업부별 Before/After 한 줄씩** (예시 I). 삼성전자처럼 5개 사업부면 5줄, 각 줄에 조 단위 숫자 2개.
4. **산업 논제 = 5단 메커니즘** (예시 J). 트렌드 → 병목 → 메커니즘 → 해결책 → 수혜. "AI가 성장한다" 식 모호 금지.
5. **목표가 = 밴드 + 양 끝 근거** (예시 K). "Base 24만원"만 쓰지 않고 "Base 24만원 = 12M Fwd EPS × Target 배수 Y.Yx (Peer Z.Zx 대비 -W% 디스카운트)"까지 한 문장에.

#### 🔀 위닝펀드 vs 프로 애널리스트 -- 블렌드 전략

| 구분 | 위닝펀드 (A~C) | 프로 애널리스트 (D~F) | 우리 리포트 |
|---|---|---|---|
| 톤 | 설명식, 교육적 | 단정식, 결론 선행 | **단정 + 교육 병행** |
| 숫자 밀도 | 중간 (2~4개/문단) | 매우 높음 (6~8개/문단) | **프로 따라가기** |
| 해설 | 깊음 (왜 그런가 풀어줌) | 얕음 (전제로 처리) | **위닝펀드 따라가기** |
| 소제목 | 메시지형 | 업종 용어형 | **메시지 + 용어 혼용** |
| 첫 문단 | 맥락 빌드업 | 결론 즉시 제시 | **프로 따라가기 (결론 선행)** |

**규칙**: 첫 문단은 프로 애널리스트처럼 **결론 + 수치 6개 이상**, 본문은 위닝펀드처럼 **왜 그런가 3층 파고들기**, 리스크는 프로처럼 **메커니즘 설명**.

#### 📋 섹션별 첫 문단 템플릿 (빈칸 채우기 강제)

**s01 투자의견 -- 첫 문단 템플릿**:
```
## 투자의견: {BUY/HOLD/SELL}

**목표 Bear {x}원 / Base {y}원 / Bull {z}원** | R/R {1:x} | 가중 기대수익 **{+X%}**

### 한 문장 투자 논문
{종목명}은 {핵심 구조 1}과 {핵심 변곡점 2}이라는 {긍정 시나리오}가 진행 중이지만, {핵심 리스크 1} + {핵심 리스크 2} 상태에서 R/R이 {1:x}에 {충족/미달}하므로 {현 보유/신규 진입} 을 {BUY/HOLD/SELL}한다.
```

**s02 투자포인트 -- 각 포인트 첫 문단 템플릿**:
```
### 포인트 N. {주장 한 문장}

{연도}년 {핵심 수치} (YoY {±X%}) 는 {의미 해석}을 시사한다. **왜 그런가?** {원인 1층위} → {원인 2층위} → {근본 원인 3층위}. **결과적으로** {투자 판단 함의}. **시장이 놓친 것**: 컨센서스 {X개사 중 Y개}가 이 관점을 {반영/미반영}, 리레이팅 여력 {+X%}.
```

**s08 재무분석 -- 첫 문단 템플릿**:
```
## 재무 분석

### {YYYY}년 {핵심 변화 메시지} -- 왜 {수치 1}이 나왔나

{YYYY}년 연결 매출은 {X}조(YoY {±%}), 영업이익 {Y}조(YoY {±%})로 {증가/감소}했다. {왜 이 수치인가?} 원인 1층위는 {표면 원인}, 2층위를 파고들면 {구조 원인}, 3층위 근본 원인은 **{본질 원인}**이다. **결과적으로** {다음 연도 전망}. {추가 문단}.
```

**s09 밸류에이션 -- 첫 문단 템플릿**:
```
## 밸류에이션

### Why the {PER/PBR/EV-EBITDA/DCF/SOTP} Method?

{종목명}은 {업종 특성 1} + {업종 특성 2} + {업종 특성 3} 때문에 {선택한 방법}이 가장 적합하다. 다른 방법들은 왜 부적합한가? {대안 A}는 {A의 단점}. {대안 B}는 {B의 단점}. 반면 **{선택한 방법}은 {핵심 장점 3가지}를 동시에 해결**한다. 실제로 {구체적 데이터 예시}.
```

**s13 투자 논문 -- 첫 문단 템플릿**:
```
## 투자 논문 (3대 근거)

### 한 문장 투자 논문

> {종목명}은 {긍정 요소 1}과 {긍정 요소 2}이라는 {호재}가 진행 중이지만, {부정 요소 1} + {부정 요소 2} + {부정 요소 3}이 이미 주가에 반영된 상태에서 R/R이 {1:x}에 {충족/미달}하므로, 현 보유자는 {HOLD/BUY/SELL}, 신규 진입은 {조건} 을 권고한다.

### 3대 근거

**근거 1: {주장} -- 왜 이것이 {중요/위험}한가?**
{구체 데이터 3~5개}. **그 원인을 분석하면** {원인 분석}. 이 프리미엄/할인이 정당화되려면 {조건}. **그러나** {반대 시나리오}.
```

**이 템플릿 빈칸 채우기 방식이 LLM 준수율을 60% → 90%로 끌어올린다.** 자유 서술 대신 구조 강제.

---

### 💎 s02 투자포인트 — 필수 구조 (최소 2000자)

**각 포인트는 4줄 구조 엄수:**

```markdown
**포인트 N. [명확한 주장 한 문장]**

- **주장**: [이 포인트가 왜 중요한지 한 줄]
- **근거 숫자**: [구체적 수치 + 추이(3년) + 출처 (financial_summary.json, 사업보고서 [0], 웹 검색)]
- **왜 주가를 올리는가**: [EPS 업사이드 또는 리레이팅 트리거 또는 환원 증가 중 하나 지목 + 구체적 %  숫자]
- **시장이 놓친 것**: [컨센서스 26개사 중 몇 개가 언급? 반영도? 리레이팅 여력 %]
```

**포인트 3~5개. 각각 구체적 숫자 + "시장이 놓친 것" 필수.**

마지막에 **안정성 체크 1개** (Altman Z / 부채비율 / 이자보상배율 / 신용등급) + **요약 테이블**:

```markdown
| # | 포인트 | 근거 핵심 숫자 | 기여 업사이드 | 시장 인지도 |
|---|---|---|---|---|
| 1 | ... | ... | +X% | 낮음 |
| 2 | ... | ... | +X% | 중간 |
| 3 | ... | ... | +X% | 매우 낮음 |
```

**❌ 나쁜 예 (v3 이전 수준):**
> "성장성이 좋다. 배당도 늘고 있다. 재무도 튼튼하다."

**✅ 좋은 예 (기아 리포트 수준):**
> **포인트 1. 순현금 19.25조 = 주당 49,340원 — 시총의 33%가 'EV 차감 대상' 현금 덩어리**
> - 주장: 기아의 실질 사업부 가치는 시장이 생각하는 것보다 47% 저렴하다.
> - 근거 숫자: 2025년말 순현금 +19.25조(현금성자산 − 차입금), 시총 58.17조의 33% 차지 (발행주식 3.97억주 기준 주당 49,340원). EV/EBITDA 2.87x (Peer 평균 4.9x 대비 -41% 디스카운트). 순현금 4년간 +13조 누적.
> - 왜 주가를 올리는가: 시장은 블렌드 DCF 기반 목표가를 계산하며 순현금의 절반만 반영한다. EV 차감 100% 반영 시 적정주가 +25k 추가.
> - 시장이 놓친 것: 실질 사업부 PBR은 현금 제외 시 약 0.5x에 불과. 이 관점을 언급하는 리포트는 26개사 중 4~5곳에 그침.

---

### 💎 s03 회사개요 — 필수 10개 요소 (최소 2500자)

**STEP 2.5의 Grep 10개 결과를 전부 반영**해야 한다. 아래 10개를 모두 포함:

```markdown
## 회사 개요

1️⃣ **한 문단 요약** (최소 3문장)
- 회사 정의 + 업계 위치 + 2025년 핵심 수치(매출/영업이익/판매대수/글로벌 순위)
- 본사 주소 + 주력 생산거점 + 사업보고서 직접 인용 1회

2️⃣ **사업 구조**
- 단일 사업부문이면 그렇게 명시 + 세그먼트별 OP 비공개 사실 기재
- 회사가 자사를 어떻게 정의하는지 [0] 직접 인용
- 최근 정관 개정/신규 목적사업 추가 (있으면 필수 언급)

3️⃣ **지역별 매출 테이블** (STEP 2.5 Grep #1 결과)
| 지역 | 순매출(조) | 비중 | M/S | 판매대수 | YoY |
|---|---|---|---|---|---|
| 국내 | XX | XX.X% | XX.X% | XX만대 | +X.X% |
| 북미 | ... | ... | ... | ... | ... |
...

- 최소 4~5개 지역
- **각 지역 M/S는 반드시 [0] 주요_계약에서 Grep으로 확보** (Grep #2)
- 해외 비중 % 정확히 (70% 같은 추정치 금지)

4️⃣ **프리미엄화 증거** (Grep #4, #5, #6)
- ASP 3년 추이 ("국내 ASP 32.2 → 33.6 → 34.9백만원 (+5.4%)")
- RV 판매 비중 지역별 추이
- 친환경차 비중 지역별 추이 3년

5️⃣ **밸류체인 + 공급망**
- 원재료 (핵심 공급처 기재)
- 생산능력 지역별 (만대 or 만톤 단위)
- 주요 생산거점 특이점 (신규 공장, 증설 계획)
- 판매 채널

6️⃣ **최대 고객 구조**
- B2C vs B2B 비중
- Top 3 고객 집중도 (공시 시)
- 공시 없으면 "공시 미기재" 명시

7️⃣ **환율·관세 노출** (있는 경우)
- USD/EUR/CNY 민감도 공시 수치 [0]
- 관세 리스크 회사 단독 영향 추정

8️⃣ **신용등급 상향 궤적** (Grep #7) — 있으면 반드시
| 평가사 | 2년 전 | 현재 | 방향 |
|---|---|---|---|
| Moody's | ... | ... | ... |
| S&P | ... | ... | ... |
| 한국신용평가 | ... | ... | ... |
| NICE | ... | ... | ... |
| 한국기업평가 | ... | ... | ... |

9️⃣ **지적재산권 자산** (Grep #8) — 있으면
- 특허 XXX건 (국내/해외 분해)
- 디자인 XXX건
- 주요 기술 분야

🔟 **전년 대비 변화 시그널 — [0] vs [2] Diff 최소 3개**
1. 신규 명시된 사업/계약/R&D
2. 정관 개정 내용 (목적사업 추가)
3. 강조점 변화 (전략 재편)
```

**s03의 핵심 원칙:**
- STEP 2.5 Grep 결과 없이 s03을 쓰지 말 것
- "해외 매출 70%" 같은 추정 표현 금지 → 정확한 수치
- 사업보고서 직접 인용 **s03 단독 최소 3회** (전체 리포트 기준 **5회 이상** 분산, STEP 6 A 블록과 동일 기준)

---

### 💎 s04 산업/시장 — 필수 8개 요소 (최소 2500자)

**기아 리포트 수준으로 쓰려면 아래 8개 블록이 전부 필요하다:**

```markdown
## 산업/시장

1️⃣ **글로벌 시장 규모 + 구조 한 문단** (웹 검색 결과)
- 시장 규모 ($X조), 글로벌 판매량, CAGR (가치 기준 vs 판매량 기준 구분)
- 출처 명시 (Mordor, S&P Mobility, Statista, Gartner 등)
- "가치 증대가 판매량인가 믹스인가" 명시

2️⃣ **본 종목의 글로벌 포지셔닝 한 문단**
- 글로벌 생산 점유 %
- 세계 몇 위권
- 지역별 M/S 정리 (STEP 2.5 Grep #2 결과)
  예: "기아 단독 글로벌 3.4%, 지역별 M/S: 국내 34.2%, 미국 5.1%, 유럽 4.0%, 인도 6.4%, 중국 0.3%"

3️⃣ **구조적 변화 3개 (각각 수치 포함)**
- 각 변화에 "YoY %", "시장 규모 $", "본 종목 영향" 3요소
- 최소 1개는 본 종목에 Bull, 최소 1개는 Bear 균형
- 웹 검색 출처 명시 (IEA, CNBC, Reuters 등 2026년 최근 뉴스)

4️⃣ **규제/정책 환경 테이블** (최근 6개월)
| 지역 | 변화 | 본 종목 영향 |
|---|---|---|
| 미국 | ... | ... |
| 유럽 | ... | ... |
| 한국 | ... | ... |
| 기타 | ... | ... |

5️⃣ **"왜 지금?" (Bull 3개)**
- 3개 각각 1문단
- 밸류에이션 저평가 근거 1개 필수
- 실행 가능한 리레이팅 트리거 1개 필수
- 재무 방어막 1개 필수

6️⃣ **"왜 조심해야?" (Bear 3개)**
- 3개 각각 1문단
- 실제 진행 중인 리스크 구체 수치
- 사이클 위치 판단
- 내러티브 증명 소요 시간

7️⃣ **업종 핵심 KPI 테이블 (본 종목 실제 수치)**
| KPI | 2023 | 2024 | 2025 | 2026E | 평가 |
|---|---|---|---|---|---|
| (업종 특화 KPI 5~6개) | ... | ... | ... | ... | ... |

- 자동차: 글로벌 판매대수, ASP, 미국 M/S, 친환경 비중, OPM
- 반도체: Wafer Start, ASP, CapEx, 재고, 가동률
- 바이오: R&D비/매출, 파이프라인 수, 특허 잔여년
- 지주: NAV 할인율, 자회사 기여도
- 리츠: NOI, 공실률, DPU

8️⃣ **업종 내 포지셔닝 한 문단 — "이 종목만의 서사"**
- "XX 업종에서 본 종목은 [고유 포지션]에 있다"
- 이 포지션이 Bull/Bear 양면 서사를 동시에 만드는 구조인지
- 리레이팅 트리거 타이밍
```

**s04의 핵심 원칙:**
- 산업 일반론만 쓰지 말 것 → "이 산업 + 이 회사의 위치" 결합
- 경쟁사별 수치 비교 최소 1회 (본 종목 vs 도요타/SK하이닉스/삼성 등)
- 균형: Bull 3 / Bear 3 엄수
- **[v4.9 신설 -- 예시 J 적용] 산업 논제는 5단 메커니즘** 필수:
  1. **거대 트렌드** (예: "AI LLM 매개변수 급증")
  2. **병목 현상** (예: "HBM 용량 한계 드러남")
  3. **수요 메커니즘** (예: "KV 저장 용량 필요")
  4. **해결책/신제품** (예: "HBF 등장")
  5. **수혜 기업** (예: "낸드 업체 + 하이브리드 본딩 장비")
  - ❌ "AI가 성장하면서 반도체 수요가 증가한다"
  - ✅ 위 5단 중 최소 4단은 구체 기술/수치로 연결

---

### s01 투자의견 (최소 300자)
- BUY/HOLD/SELL + 목표가 Bear/Base/Bull + R/R 비율
- 한 문장 투자 논문
- 핵심 근거 3개 (s02 포인트와 매핑)
- **[v4.9 신설 -- 예시 K 적용] 목표가는 "밴드 + 양 끝점 산정 근거 한 줄"** 의무
  - ❌ "Base 24만원"
  - ✅ "Base 245,000원 = 2026E EPS 22,316원 × Target PER 11.0x (5Y 평균 12.1x 대비 -9% 할인, HBM 공급 과잉 리스크 반영)"
  - Bear/Bull 도 동일 형식 (각각 다른 Target 배수 + 할인/프리미엄 사유)

### s05 경쟁 구도 + Peer Comparison (최소 500자)
- **Q7: Peer 선정 근거 한 문장 필수**
- Peer 테이블 최소 5개 (본 종목 + 국내 3~4 + 글로벌 2)
- 경쟁 우위/열위 (2년 내 추월 가능성 답변)

### s06 경제적 해자 (최소 350자)
- 5가지 해자 유형 중 해당 판정 (브랜드/비용우위/네트워크/전환비용/무형자산)
- 각 해자 증거 (점유율, 마진 프리미엄, 특허 수)
- 해자 강도: Wide / Narrow / No Moat

### s07 경영진 (최소 300자)
- 최대주주, 지배구조 (financial_summary.json shareholders)
- 경영진 평가 (과거 의사결정)
- 내부자 매매 동향

### s08 재무분석 (최소 600자)
- 5-Layer 분석 (표면→원인→지속성→리스크→밸류 입력)
- DuPont 3단계 분해 (NPM × AT × Lev)
- 원가율/판관비율 추이 + 3단계 "왜?"
- 부채비율, 이자보상배율, 운전자본 변동
- OCF vs 영업이익 괴리 원인
- **재무 최대 위험 1가지 명시**
- Red Flag 10개 체크리스트
- Altman Z-Score
- Forward 6년 테이블 (Q10)
- **[v4.9 신설 -- 예시 I 적용] 다사업부 종목은 "사업부별 Before → After 조 단위 대비"** 첫 문단 의무:
  - ❌ "전체 영업이익이 큰 폭 개선되었다"
  - ✅ "사업부별 영업이익(YY → YY+1)은 **반도체 2.9조 → 16.4조**, SDC 0.9조 → 2.0조, MX/NW 2.1조 → 1.9조, VD/DA 0.2조 → -0.6조, 하만 0.4조 → 0.3조. 반도체가 +13.5조 점프하며 전체를 견인한 반면, VD/DA는 적자 전환"
  - 5개 이상 사업부가 있으면 **5줄 한 단락**에 각 사업부 한 줄씩

### s09 밸류에이션 (최소 700자)
**Q1~Q3, Q5 프로토콜 필수**:
- **[필수] Q1 DCF 가정 테이블** (WACC/g/Beta/Rf/ERP/Kd 근거까지)
- **[필수] Q2 민감도 테이블** (매출 CAGR × OPM 2x2 이상)
- **[필수] Q3 역사적 PER/PBR 밴드** (5년, 백분위)
- **[적자기업] Q5 PSR/EV/EBITDA/P/B 병행**
- FCF Yield + 안전마진 + Peer 상대 비교
- "이 밸류에이션이 정당화되려면 어떤 성장률?" 답변
- 현재가 +1σ 초과면 리레이팅 근거

### s10 매크로 리스크 (최소 400자)
- 매크로 리스크 3~5개 + 각 민감도 수치
- "주가를 반토막 낼 수 있는 1가지" 시나리오
- 금리/환율/원자재/규제 중 본 종목 최대 영향 변수

### s11 카탈리스트 타임라인 (최소 300자)
- 카탈리스트 테이블 최소 4개 (시기/이벤트/영향/확률)
- 사업보고서 [0] 주요_계약에서 숨은 카탈리스트 최소 1개
- 부정적 카탈리스트 2개 균형
- **[v4.9 신설 -- 예시 H 적용] 리스크/카탈리스트 수치는 "최대치 → 감경 경로 → 최종 예상치" 3단**:
  - 예: "과징금 최대 1조원 → 감경 기준 1/2 + 내부통제 75% 추가 적용 → 최종 3,000억 하회 예상"
  - 예: "관세 최대 -15%p 영향 → 현지 생산 비중 47% + 멕시코 JV 오프셋 → 최종 순영향 -4%p"
  - "걱정된다"로 끝내지 말고 **감경 메커니즘 명시 후 최종 숫자까지 제시**

### s12 Bear/Base/Bull + R/R (최소 400자)
- 3시나리오 구체 숫자 (매출/OP/EPS/EV/EBITDA)
- 각 확률 배정
- R/R 계산 + 하방/상방 비율

### s13 투자 논문 (최소 400자)
- 한 문장 논문 + 3대 근거
- 각 근거에 데이터

### s14 숏 논거 3가지 + 반박 (최소 400자)
- "이 주식을 사면 안 되는 이유" 3개
- 각각 데이터 기반 반박
- 반박 강도 3개 중 N개 강함 → BUY/HOLD/SELL 매핑

### s15 Beat/Miss (최소 300자)
- 최근 분기 실적 vs 컨센서스
- Q4 일회성 비용 체크 (정상화 이익 병기)
- 서프라이즈/미스 원인

### s16 애널리스트 컨센서스 (최소 400자, v4.8 컨센서스 추이 표 필수)
- financial_summary.json consensus 섹션 활용
- 주요 증권사 목표가 테이블 최소 5개
- 평균 + 스프레드
- **Q4: 내 추정 vs 컨센 델타**

**🚨 v4.8 필수 삽입 -- 컨센서스 추이 표** (`_wisereport.json.consensus_trajectory_2026E` 기반):

```markdown
### 컨센서스 추이 (2026/12 영업이익 기준)

| 시점 | 컨센서스 OP | EPS | PER | ROE | 변화 |
|---|---|---|---|---|---|
| 1년 전 | {1년전_OP}조 | {EPS}원 | {PER}x | {ROE}% | 기준 |
| 3개월 전 | {3개월전_OP}조 | ... | ... | ... | {vs 1년전 %} |
| 1개월 전 | ... | ... | ... | ... | ... |
| 1주 전 | ... | ... | ... | ... | ... |
| **오늘** | **{오늘_OP}조** | **{EPS}원** | **{PER}x** | **{ROE}%** | **{vs 1년전 %}** |

[해석 문단]
3개월 전 대비 컨센서스가 {+X%} {상향/하향}되었다. 이는 {메가트렌드 상향/하향 사이클}을 의미하며,
{구체적 요인 (DDR5 가격 폭등, 1Q 실적 서프라이즈 등)} 에 기반한다.
본 리서치는 이 상향 궤적을 {수용/보수 조정} 하여 2026E OP {X}조를 Base로 설정한다.
```

**해석 원칙**:
- 3개월 전 대비 **+50% 이상 상향** → "메가트렌드 상향 국면" 으로 리포트 전체 Bull 스탠스 채택
- 3개월 전 대비 **-30% 이상 하향** → "실적 하향 사이클" Bear 반영
- **컨센서스 추이 표 누락 시 STEP 6 B11 FAIL** (verify_numbers.py 자동 검출)

### s17 수급 분석 (최소 300자)
- 외국인/기관/개인 20일 순매수 금액
- **Q8: 수급 맥락화** (일평균 거래량/유동주식 대비 %)
- 매집/분산 판정
- **[v4.9 신설 -- 예시 G 적용] 수급은 "누가 팔고 누가 샀는가" 숫자 4~6개를 한 문단에 병치**:
  - ❌ "외국인이 매도세를 보였다"
  - ✅ "외국인은 KOSPI를 2.1조원 순매도, 본 종목도 3,000억원 순매도. **반면** 국내 기관은 KOSPI를 3.4조원 순매수, 본 종목도 3,340억원 순매수. 기관 순매수 강도가 주간 +X% 강해지는 양상"
  - **대비 구조("A는 ~했지만, 반면 B는 ~")** 필수

### s18 주주환원 (최소 300자)
- 배당 5년 추이 (DPS/배당수익률/배당성향)
- 자사주 매입 계획
- TSR vs 공언 목표
- 회사 공식 주주환원 정책 [0] 인용

### s19 Trust / Worry / Watch (최소 400자)
- Trust 4~5개 (확고한 강점)
- Worry 4~5개 (우려 사항)
- Watch 4~5개 (모니터링 포인트)

### s20 실행 계획 (최소 400자)
- **Q9: 변동성/베타 기반 포지션 사이징**
- 진입 3단계 + 분할매수
- 손절 2가지 (기술적 + 펀더멘털)
- 목표 Bear/Base/Bull 진입가

### s21 신뢰도 & 한계 (최소 200자)
- 신뢰도 점수 (데이터 완전성 기반)
- 누락 필드 / 추정 사용 부분
- 사용 출처 목록

---

## 🎯 퀀트 필수 프로토콜 Q1~Q10

**아래 10개 요건이 모두 충족되지 않은 리포트는 "블로그"다.**

### Q1: DCF 가정 테이블 (s09)
```markdown
| 가정 | 값 | 근거 |
|---|---|---|
| Rf | 3.5% | 국고채 10년물 |
| ERP | 5.0% | KR 시장 평균 |
| Beta | 1.2 | 120일 회귀분석 (Q9 참조, R² 공개) |
| Kd | 3.8% | 이자비용/차입금 |
| **세율 t** | **흑자: 22% / 적자: 0%** | **적자기업은 실효세율 0% — 22% 적용은 오류** |
| D/V | 30% | 시총+순차입금 |
| WACC | 8.5% | Ke×E/V + Kd×D/V × (1-t) |
| 예측 FCF CAGR | 12% | 2026~2030E |
| Terminal g | 2.0% | 장기 GDP |
```
**WACC와 g 중 하나만 공개하면 실격.**
**⚠️ 에코프로 사례: 적자기업에 세율 22%를 기계적으로 적용해서 세후 Kd를 줄인 오류. 적자기업의 실효세율은 0 또는 음수이므로 세후 Kd 조정을 **하지 않음**. 세율 0%로 계산해야 정확.**

### Q2: 민감도 테이블 (s09)
```markdown
| 매출 CAGR \ OPM | 6% | 8% | 10% |
|---|---|---|---|
| 5% | 75,000 | 85,000 | 95,000 |
| 10% | 85,000 | 100,000 | 115,000 |
| 15% | 95,000 | 115,000 | 135,000 |
```
**스프레드 ±30% 넘으면 "가정 의존적" 명시.**

### Q3: 역사 밸류 밴드 (s09)
STEP 1.7 계산 결과. 현재가 +1σ 초과 시 리레이팅 근거 필수.

### Q4: 컨센서스 델타 (s02 또는 s16)
```markdown
| 항목 | 컨센서스 | 내 추정 | 델타 | 근거 |
|---|---|---|---|---|
| 2026E 매출 | ... | ... | +X% | ... |
| 2026E OP | ... | ... | +X% | ... |
| 2026E EPS | ... | ... | +X% | ... |
```
**델타 전부 ±2% 이내 → edge 없음, HOLD.**

### Q5: 적자기업 프로토콜 (s09)
PER 무의미 종목(적자 또는 PER 50x+)은 PSR + EV/Sales + EV/EBITDA + P/B + 정상화 PER 병행.

### Q6: Q4 일회성 (s08 또는 s15)
일회성이면 "정상화 이익" 별도 계산. Bear/Base/Bull은 정상화 EPS로.

### Q7: Peer 선정 근거 (s05)
첫 문장에 한 줄 근거. "동일 [업종] + 시총 [범위] + 사업 [특성]으로 [A,B,C]를 1차 Peer로, [X,Y]를 글로벌 벤치마크."

### Q8: 수급 맥락화 (s17)
"기관 20일 순매수 102만주 = 일평균 50만주의 2일치 = 유동주식 1,200만주의 8.5%"

### Q9: 변동성·베타 포지션 (s20)

**Beta는 반드시 정식 회귀분석 결과**를 사용한다. "변동성 × 1.5" 같은 추정 금지.

```python
# 시장 기준: 코스닥150 ETF "229200" (코스닥 종목) 또는 코스피200 ETF "069500" (코스피 종목)
from kis_api import get_daily_price
import numpy as np

stock_code = "035900"   # 본 종목 6자리 코드 (스킬 호출 시 확정)
mkt_code   = "229200"   # JYP는 코스닥 → 229200. 코스피 종목이면 "069500"

stock_daily = get_daily_price(stock_code, 100)
mkt_daily   = get_daily_price(mkt_code, 100)

# kis_api.get_daily_price()는 list[dict] 반환, 키는 한글('날짜'/'종가' 등)
stock_closes = np.array([float(r["종가"]) for r in stock_daily[::-1]])
mkt_closes   = np.array([float(r["종가"]) for r in mkt_daily[::-1]])

stock_rets = np.diff(np.log(stock_closes))
mkt_rets   = np.diff(np.log(mkt_closes))

# 길이 불일치 방지 (휴장일/오류 등)
n = min(len(stock_rets), len(mkt_rets))
stock_rets, mkt_rets = stock_rets[:n], mkt_rets[:n]

cov     = np.cov(stock_rets, mkt_rets)[0, 1]
var_mkt = np.var(mkt_rets)
beta    = cov / var_mkt
corr    = np.corrcoef(stock_rets, mkt_rets)[0, 1]
r2      = corr ** 2
vol_annual = float(np.std(stock_rets) * np.sqrt(252))

print(f"[Q9] Beta={beta:.2f} (R²={r2:.3f}) σ_annual={vol_annual*100:.1f}%")
```

```markdown
| 항목 | 값 |
|---|---|
| 120일 연환산 변동성 (σ) | 52% |
| 코스닥150 대비 Beta | **1.38** (R² 0.643) |
| 특이 변동성 (idiosyncratic) | 58% (총 변동성의 60%) |
| 권고 포지션 | 2~3% |
| VaR (95%, 1일) | -4.8% |
```

- σ>40% → 포지션 3% 이하
- σ>90% → **포지션 2% 이하 강제** (극초고변동성)
- **Beta 값과 함께 R² 공개 필수** — R² < 0.3이면 "Beta 신뢰도 낮음" 명시

**⚠️ 에코프로 사례: 이전 리포트에서 "Beta 1.8 추정 (변동성 97% × 1.5)"로 근거 없이 계산했으나, 실제 정식 회귀분석 결과는 1.38. 과대추정 0.4 오류 발생.**

### Q10: Forward 6년 테이블 (financials)
```json
"headers": ["항목","2023","2024","2025","2026E","2027E","2028E"]
```
3년 역사 + 3년 Forward.

---

### Q11: 지주회사 체크리스트 (에코프로 수술 교훈)

**종목이 지주회사인 경우 다음 8단계를 반드시 수행한다:**

1. **상장 자회사 실시간 시총 조회** — KIS API로 직접 조회 (추정 금지)
   ```python
   from kis_api import get_current_price
   sub_info = get_current_price("자회사_종목코드")
   mcap = sub_info["시가총액"]  # 억원
   ```

2. **각 자회사 지분율 × 실제 시총 = 지주사 몫 계산**
   ```
   에코프로비엠 46% × 19.71조 = 9.07조
   에코프로에이치엔 31% × 0.63조 = 0.20조
   에코프로머티 51% × 5.19조 = 2.65조
   상장 자회사 소계 = 11.91조
   ```

3. **비상장 자회사 가치 범위 추정** (상/하한 명시)
   - 각 비상장 자회사의 매출 × Peer PSR
   - 또는 설비 가치 기반 추정
   - "3~5조" 같은 범위로 투명하게 표시

4. **순차입금 차감 → Net NAV 도출**
   ```
   NAV = 상장 자회사 몫 + 비상장 자회사 - 순차입금
   ```

5. **지주사 프리미엄/할인 % 계산**
   ```
   프리미엄 = (시총 - NAV) / NAV × 100
   음수면 할인, 양수면 프리미엄
   ```

6. **한국 증시 지주사 정상 할인율 비교**
   - 삼성물산 25~35% / SK 40~55% / LG 30~45% / 한화 35~50%
   - 한국 지주사 평균 **30~50% 할인**
   - 위 범위 밖이면 "이례적" 명시

7. **지주사 본체 별도 재무 분석** (s09에 별도 블록)
   - 지주사 단독 매출 구성: 무역 / Shared SVC / 배당수익
   - 지주사 본체의 실물 가치 추정 (OP × 멀티플)
   - 이게 지주 시총의 어느 비중을 설명하는지

8. **그룹 합산 PSR / EV/EBITDA 계산** (새 분석 각도)
   ```
   그룹 3사 합산 시총 / 합산 매출 = 그룹 PSR
   섹터 평균 대비 몇 배 프리미엄인지
   ```

**⚠️ 에코프로 v1 실패 사례**: 자회사 시총을 "추정 25조/1조/0.5조"로 써놓고 실제 조회 안 함. 실제는 19.71조/5.19조/0.63조로 에코프로머티 **5배 오차** 발생. NAV 전체 신뢰도 붕괴. 반드시 실시간 API 조회.

---

### Q12: Altman Z-Score 5 구성요소 전부 수치 공개

**"추정 1.95" 같은 식으로 얼버무리지 말 것.** 5개 요소를 실제 수치로 대입해서 표로 공개.

```markdown
| 요소 | 공식 | 계산 | 값 | 가중치 | 기여 |
|---|---|---|---|---|---|
| X1 | 운전자본 / 총자산 | 31 / 97,787 | 0.0003 | 1.2 | 0.0004 |
| X2 | 이익잉여금 / 총자산 | 8,938 / 97,787 | 0.0914 | 1.4 | 0.128 |
| X3 | EBIT / 총자산 | 2,138 / 97,787 | 0.0219 | 3.3 | 0.072 |
| X4 | 시가총액 / 총부채 | 198,776 / 52,914 | 3.7566 | 0.6 | 2.254 |
| X5 | 매출 / 총자산 | 34,130 / 97,787 | 0.3490 | 1.0 | 0.349 |
| **Z** | | | | | **2.80** |
```

**X4(MV/TL) 기여도가 전체 Z의 50% 초과면 "시총 의존성" 경고** — 시총 정상화 시 Z가 어떻게 변하는지 감도 분석 필수.

**⚠️ 에코프로 사례**: Z = 2.80이 회색 안정 구간처럼 보였으나, X4 기여 80% 발견. 시총 -45% 조정 시 Z = 1.80 부실 구간 진입하는 자기강화 악순환 구조. 이런 통찰은 5 요소 공개 없이는 안 나온다.

---

### Q13: 숫자 정직성 체크리스트

리포트 제출 전 모든 "숫자"가 검증 가능한지 다음을 확인:

- [ ] 모든 "시가총액", "EPS", "BPS", "PER", "PBR"은 **KIS API 실시간 조회값** (추정 금지)
- [ ] 자회사 시총은 **각자 실시간 조회** (비교 시 반드시 추정 아닌 실값)
- [ ] Beta는 **회귀분석 결과** + **R² 공개** (변동성 × 임의 계수 금지)
- [ ] Altman Z는 **5개 요소 모두 수치 대입 공개** (추정 금지)
- [ ] WACC 세율: **적자기업=0%, 흑자기업=실효세율** (22% 기계적 적용 금지)
- [ ] 주식 희석률: **주식분할 조정 후 계산** (분할 전 주식 수 직접 비교 금지)
- [ ] 컨센서스 데이터: 웹 검색 결과가 있으면 **반드시 본인 추정과 비교**
- [ ] 모든 "추정" 표현에 **범위와 근거** 명시 (단일 수치 추정 금지)
- [ ] 재고평가손/일회성 이익: **실제 금액**을 사업보고서에서 확인 (숫자 없이 추정 금지)
- [ ] EV/EBITDA, EV/Sales: **financial_summary.json에 내장 지표 사용** (있는데 누락 금지)

**⚠️ 에코프로 v1 실패 사례 모음**:
- 자회사 시총 "25조/1조/0.5조" 추정 → 실제 19.71/5.19/0.63 (5배 오차)
- Altman Z "추정 1.95" → 실제 계산하면 2.80 (0.85 오차)
- Beta "1.8 추정" → 정식 회귀 1.38 (0.4 오차)
- WACC 세율 22% 기계 적용 → 적자기업 0%가 정확
- 주식 희석 "5.66배" → 분할 조정 후 실제 1.13배 (13.3%)
- EV/EBITDA 48.45x를 financial_summary에 있었는데 활용 누락

---

### Bull 시나리오 일관성 규칙 (에코프로 수술 교훈)

**Bull 시나리오의 PBR은 반드시 Q3 역사 밴드의 "정상화 PBR" 또는 그 근처**여야 한다.

**예시 (에코프로)**:
- s09에서 "정상화 PBR 5x" 주장
- Bull 시나리오도 PBR 5~7x 범위여야 논리 일관
- **Bull에서 PBR 9.5x 적용하면 "정상화 5x" 주장과 모순** → 레이팅 재검토 대상

**체크**:
- Bear PBR: 역사 -1σ 수준
- Base PBR: 역사 평균 수준
- Bull PBR: 역사 +1σ 이하 (현재 역사 +1σ 초과면 Bull에서도 초과 금지)

**⚠️ 에코프로 사례**: v1에서 s09는 "정상화 PBR 5x"라 주장하면서 Bull 시나리오는 PBR 9.5x 유지로 목표가 150,000원 산정. 내부 모순. v2에서 Bull PBR 7x로 수정 → 108,000원.

---

## 기관 톤 가이드라인 (에코프로 수술 교훈)

리포트는 골드만삭스/모건스탠리 수준의 기관 톤을 유지한다.

### 금지 표현 (선동적, 리테일 톤)

| 🔴 금지 | 🟢 권장 |
|---|---|
| "실질 SELL" | "HOLD with downside bias" 또는 "SELL" |
| "투기 구간", "투기 진입" | "Elevated speculative risk" 또는 "High beta exposure" |
| "테마주 과열", "극단적 과열" | "Valuation appears stretched" |
| "황제주", "국민주" | "Retail favorite" (최대한 중립) |
| "떡상", "폭망", "코인처럼" | "Sharp rally", "Significant decline" |
| "사실상 매도" | "Recommendation biased to sell" |
| "무조건", "반드시" | "Likely", "Expected" |

**⚠️ 에코프로 v1 사례**: "실질 SELL (과열 경고)"이라는 표현이 커버 태그라인에 그대로 노출. 이는 기관 리포트에서 쓰지 않는 용어. v2에서 "HOLD with downside bias"로 변경.

### Rating 체계

- **BUY**: 12개월 상방 +20% 이상 + R/R 1:2 이상
- **HOLD**: 상방 0~20% 또는 R/R 1:1~1:2
- **HOLD with downside bias**: 상방 0% 또는 음수, R/R 1:1 미만 (에코프로 v2 케이스)
- **SELL**: 하방 -15% 이상 + R/R 음수 확실 + 단기 트리거 있음

기관 리포트는 **SELL을 남용하지 않음**. 레이팅 하향은 대부분 BUY → HOLD 순서로 진행. HOLD with downside bias가 실질 SELL 역할.

---

## analysis.json 구조

```json
{
  "meta": {"stock_name", "stock_code", "market", "country", "industry", "date", "currency"},
  "price": {"current", "change_pct", "high_52w", "low_52w", "market_cap", "market_cap_num", "per", "pbr", "eps", "bps", "dividend_yield", "daily_prices":[]},
  "opinion": {"rating", "type", "portfolio_role", "target_bear", "target_base", "target_bull", "risk_reward"},
  "segments": [{"name", "pct", "outlook"}],
  "financials": {"headers", "rows", "source"},
  "quarterly": {"headers", "rows", "year", "note"},
  "supply": {"foreign", "institution", "individual", "days", "comment"},
  "peers": [{"name", "market_cap", "per", "pbr", "note", "highlight"}],
  "catalysts": [{"date", "event", "impact"}],
  "sections": { /* 아래 21개 키를 정확히 사용 */ }
}
```

**⚠️ sections 키 이름 — 정확히 이 21개 (줄이거나 바꾸면 리포트에 빈 칸):**

```
s01_opinion              s08_financial            s15_beat_miss
s02_investment_points    s09_valuation            s16_consensus
s03_company_overview     s10_macro                s17_supply
s04_industry             s11_catalysts            s18_shareholder_return
s05_competition          s12_scenarios            s19_trust_worry_watch
s06_moat                 s13_thesis               s20_action_plan
s07_management           s14_short_thesis         s21_reliability
```

**흔한 실수:**
- ❌ `s02_invest_points` → ✅ `s02_investment_points`
- ❌ `s03_company` → ✅ `s03_company_overview`
- ❌ `s11_catalyst` → ✅ `s11_catalysts` (복수)
- ❌ `s12_scenario` → ✅ `s12_scenarios` (복수)
- ❌ `s15_earnings` → ✅ `s15_beat_miss`
- ❌ `s18_shareholder` → ✅ `s18_shareholder_return`
- ❌ `s20_action` → ✅ `s20_action_plan`

**섹션 본문 표 — 마크다운 파이프 테이블:**
```markdown
| 항목 | 2023 | 2024 | 2025 |
|---|---|---|---|
| 매출 | ... | ... | ... |
```
구분자 줄(`|---|---|`) 반드시 필요.

**통화 주의:**
- KR: currency="원", 숫자 뒤 (54,100원)
- US: currency="$", 숫자 앞 ($54.10)

**market_cap 파싱:**
- "조" (한국), "B" (달러), "T" (조 달러) 지원
- `market_cap_num`은 **억 단위** 정수 (KIS API의 `시가총액` 필드와 일치해야 함)

---

## STEP 5: generate_all.py 실행

```bash
python scripts/generate_all.py scripts/analysis_{종목명}.json
```

→ **단일 출력 파일**: `output/{종목명}/report_{종목명}_상세.pdf`
- HTML→PDF 기반 (Playwright Chromium)
- Navy/Gold 팔레트 (`#0b2545` / `#b8922e`) 기관급 디자인
- 24~46 페이지 (종목 본문 분량에 따라 자동 분할)
- 구성: Cover (1p) + Executive Summary (1p) + 21 섹션 (자연 흐름) + Final Call (1p)
- v3 디자인 특징:
  - Cover: 네이비 그라디언트 + 골드 액센트 + Our Call 박스
  - Executive Summary: Verdict Card 6셀 + Core Thesis (이탤릭) + 21섹션 TOC
  - 21섹션: 골드 캡션 + Georgia serif 헤딩 + NYT 스타일 표 + 골드 좌측바 서브헤딩
  - 마크다운 풀 파싱 (`### 헤딩`, `**bold**`, `*italic*`, `| 표 |`, `> 인용`, `- 리스트`, `---` 구분자)
  - 이모지 자동 제거 (🔴🟢⚠️📊 등 모두 strip, ★☆는 별점용으로 보존)
  - Final Call: 네이비 더블 보더 + 골드 캡션 + 이탤릭 결론 사인오프

**v3 이전 변경 사항 (구버전 → 현재 v3):**
- ❌ 요약 PDF (10페이지 HTML→PDF) — 폐기
- ❌ 대시보드 HTML — 폐기
- ❌ Word/docx → PDF 변환 — 폐기 (마크다운 raw 텍스트 노출 + 디자인 한계)
- ✅ 단일 상세 PDF (HTML/CSS Navy/Gold)로 통합

**자동 품질 검증 9개:**
- [ERROR] → 생성 중단 (시총 10배 오류, 현재가 0원 등)
- [WARN] → 생성은 하지만 경고 출력 → STEP 5.5에서 수정

---

## STEP 5.5: [폐지] -- v4.7부터 STEP 6 Step 1a로 완전 이전

**이 단계는 수행하지 않는다.** `scripts/verify_numbers.py` 가 B1~B11 수치 정합성을 자동 검증하며, STEP 6 Step 1a에서 실행된다. LLM이 수작업으로 KIS/DART 원본과 대조하는 legacy 워크플로는 토큰 낭비이므로 **제거**됨. 바로 STEP 6으로 진입한다.

---

## STEP 6: 자체 평가 -- "통합 1회차 + 조건부 2회차" (v4.6, 2026.04 재설계)

### 🎭 페르소나 재활성화 — 자기 리포트를 찢어보는 월가 시니어

**STEP 6에 진입하는 순간, 너는 자신이 방금 쓴 리포트를 처음 보는 월가 시니어 애널리스트가 된다.** 연봉 50만 달러, CFA/CPA, 골드만삭스·모건스탠리 15년 경력의 페르소나(STEP 0)를 **더 가혹한 모드**로 재활성화한다.

너의 역할 규칙:
- **냉정하다**: "괜찮아 보인다" "합리적이다" 같은 애매한 평가 금지. 모든 판단은 숫자·인용·논리로.
- **객관적이다**: "내가 쓴 리포트"라는 감정 이입 금지. 타인이 쓴 걸 리뷰하듯 본다. 자존심을 지키려 하지 말 것.
- **가혹하다**: 신입 애널리스트가 이 리포트를 들고 왔다면, 네가 IC(Investment Committee)에서 어떻게 찢을지 상상하라. 그 수준의 비판을 자기 리포트에 적용한다.
- **"숨은 결함"을 사냥한다**: 표면적으로 잘 쓰인 부분에서도 약한 논거, 숫자 불일치, 희망적 관측을 적극 발굴한다.
- **독자 편이다**: 작성자(나)를 보호하지 말고, 이 리포트를 읽고 돈을 걸 개인 투자자를 보호한다.

**3번 읽기 원칙:**
1. **정합성 읽기** — Peer/역사 밴드/KIS 수치를 원본과 1:1 대조. 거짓말 사냥.
2. **논리 읽기** — s01 결론(BUY/HOLD/SELL)이 s02~s14 근거와 **기계적으로** 연결되는가? 빠진 고리가 있는가?
3. **냉정 읽기** — "내가 이 리포트를 IC에서 발표했을 때 시니어 파트너가 가장 먼저 물어볼 질문 3개는?" → 리포트가 그 질문에 답하고 있는가?

**수정은 "미봉책"이 아니라 "근본 수정"이다.** 문구만 다듬지 말고, 논거가 약하면 데이터를 다시 가져와 보강한다. 수치가 틀렸으면 원본으로 돌아가 재조회한다.

### 📊 v4.6 재설계 배경 -- 왜 3회 → 1회 통합인가

**v4.5까지 (3회 반복)** 의 실측 결과:

| 방식 | 발견 결함 수 | 소요 시간 | 토큰 | 비용 |
|---|---|---|---|---|
| 대한항공 v1: 메인 체크리스트 3회 | **0건** | 10분 | 30K | $0.5 |
| 대한항공 v1: 사용자 한 문장 도전 (실질 1회 서브에이전트) | **5건 치명적** | 8분 | 100K | $2 |
| 삼성전자 v1: 1회차 체크리스트만 | 2건 | 2분 | 5K | $0.2 |
| 삼성전자 v1: 2회차 서브에이전트 | **13건 치명적** | 9분 | 100K | $2 |
| 삼성전자 v1: 3회차 재호출 (없었다면) | 예상 0~2건 추가 | 9분 | 100K | $2 |

**핵심 교훈**:
- 메인 에이전트 체크리스트 반복은 **자기 일관성 편향**으로 실효성 낮음
- 서브에이전트 1회 호출이 핵심 가치의 80% 제공
- 3회차 재호출은 한계효용 급락 (1~2건 추가에 $2)

**v4.6 설계**:
- 1회차에 **"기계 검증 + 체크리스트 + 서브에이전트"** 를 통합 수행
- 2회차는 **"서브에이전트가 CRITICAL 3건+ 발견 시에만"** 조건부 실행
- 3회차 폐기
- 시간 60% 단축 ($3~5 → $1.8~3), 품질 거의 동일

---

### 🚨 절대 규칙 (v4.6 재설계)

1. **1회차는 무조건 수행.** 기계 검증(B 블록) + 체크리스트(A 블록) + 서브에이전트 호출 3단계를 모두 실행한다.
2. **1회차 서브에이전트 결과로 종료 여부 결정.** CRITICAL/치명적 결함 3건 이상이면 2회차 진입, 2건 이하면 1회차로 종료.
3. **3회차 금지.** v4.5까지 있었던 3회차는 한계효용 체감으로 폐기. "혹시 모르니 한 번 더"는 토큰 낭비.
4. **매 단계마다 `generate_all.py` 재실행**하여 PDF가 실제로 수정 내용을 반영했는지 확인한다.
5. **점수 자기기만 금지.** 수치 오류는 무조건 CRITICAL로 분류한다. "사소하지만 일단 넘어가자" 금지.
6. **서브에이전트 원문 보고 보존.** 서브에이전트 출력을 요약만 하지 말고 사용자에게 그대로 전달한다.

### 📐 1회차 3단계 통합 설계

**Step 1a (30초) -- 기계 검증**: `python scripts/verify_numbers.py {종목명}` 실행. B1~B10 10개 항목 자동 PASS/FAIL 판정. FAIL 이 있으면 먼저 수정 후 다음 단계로.

**Step 1b (1~2분) -- 메인 에이전트 체크리스트 A**: 아래 A 블록 8개 항목을 직접 점검. 빠진 구조 식별.

**Step 1c (5~8분) -- report-critic 서브에이전트 호출**: Fresh context 가혹 비평. 질적 결함 (가정 공격성, 논거 깊이, 놓친 각도) 발굴.

**Step 1d (2~5분) -- 통합 수정**: Step 1a/1b/1c 의 발견을 모두 반영하여 analysis.json 수정 → `generate_all.py` 재실행.

**총 소요: 10~15분 / 토큰 100~130K / 비용 ~$2~3** (v4.5 대비 -60%)

---

### 🔹 1회차: 통합 검증 (기계 + 체크리스트 + 서브에이전트)

**목적**: 리포트의 **모든 종류 결함을 1회 안에 발견하고 수정한다**. 기계가 잡는 것(수치 정합성), 사람이 잡는 것(형식 누락), 서브에이전트가 잡는 것(질적 결함)을 **동시에** 수행.

**역할 분담 (v4.6 재편)**:
- **Step 1a -- `verify_numbers.py` 기계 검증 (B 블록 10개)** → B1~B10 수치 정합성 자동 검증. 시총/현재가/PER/PBR/EPS/BPS/52주 고저, 수급 부호, 순현금, EBITDA/EPS 테이블, 신용등급, R/R.
- **Step 1b -- 메인 에이전트 체크리스트 A (구조 8개)** → s02/s03/s04 구조, s08/s09 테이블, 사업보고서 인용 5회+, 업종별 프로토콜 적용 등 필수 필드 존재 확인.
- **Step 1c -- `report-critic` 서브에이전트 호출 (질적 평가)** → Fresh context 가혹 비평. 가정 공격성, 논거 깊이, 숨은 모순, 놓친 각도.
- **Step 1d -- 통합 수정** → 위 3가지 발견을 모두 반영하여 analysis.json 수정 + `generate_all.py` 재실행.

```
=== STEP 6 1회차 통합 검증: {종목명} (v4.6) ===

[A. 핵심 구조 존재 확인 — 8개 항목]

- [ ] **s02 4줄 구조** (주장/근거/왜주가/시장간과) × 3~5 포인트 + 요약 테이블 존재
- [ ] **s03 STEP 2.5 Grep 결과 반영**: 지역별 매출, 시장점유율/M/S, ASP/프리미엄화, 신용등급 궤적(있으면), 판매대수 YoY 등 **Grep 10개 패턴 중 최소 4~5개 반영**
- [ ] **s04 8요소 구조**: 시장규모 + 본 종목 글로벌 포지셔닝 + 구조적 변화 3개 + 규제 테이블 + Bull 3/Bear 3 + 업종 KPI 테이블
- [ ] **s08 Forward 6년 테이블** (Q10) 존재 (3년 역사 + 3년 Forward)
- [ ] **s09 Q1 DCF 가정 + Q2 민감도 + Q3 역사 밴드** 3개 전부 존재 (적자 종목이면 Q5 PSR/EV/EBITDA 병행)
- [ ] **사업보고서 직접 인용 5곳 이상** 전체 리포트에 (형식: "사업보고서 [0]에서 '...'라고 기재")
- [ ] **전년 대비 변화점 3개 이상** s03 [0] vs [4] Diff 블록에
- [ ] **업종별 필수 프로토콜 적용** (지주=Q11 NAV, Altman Z=Q12, 숫자 정직성=Q13, 적자=Q5, Q4 일회성=Q6)

[B. 수치 정합성 원본 대조 — 10개 항목 (v4.5 신설, 삼성전자 13건 사고 재발 방지)]

**실제로 파일을 열어 대조해야 하며, "대충 맞을 것"이라는 확인은 금지.** 각 항목은 원본 파일 경로 + 검증 방법을 명시한다.

- [ ] **B1. 시가총액 정합성**: `analysis.json.price.market_cap_num` == `data_kis.json.current_price.시가총액` (단위 억원, ±1% 이내). **시총 10배 오류 자동 차단**.
- [ ] **B2. 현재가 정합성**: `analysis.json.price.current` == `data_kis.json.current_price.현재가` (정확 일치).
- [ ] **B3. PER/PBR/EPS/BPS 정합성**: 4개 지표 `analysis.json.price` vs `data_kis.json.current_price` 완전 일치. EPS 역산(현재가/PER) 검증 포함.
- [ ] **B4. 52주 고/저 정합성**: `analysis.json.price.high_52w` / `low_52w` == KIS 원본. **삼성 48,500→120,200 사고 재발 방지**.
- [ ] **B5. 수급 부호 일관성**: `analysis.json.sections.s17_supply` 서술의 외국인/기관/개인 방향이 `data_kis.json.investor_trend.{외국인_순매수, 기관_순매수, 개인_순매수}` 부호와 일치. **양수/음수 뒤집기 원천 차단**.
- [ ] **B6. 순현금/순차입 일관성**: `analysis.json.sections.s08_financial` / `s19_trust_worry_watch` 의 "순현금 N조" 서술 == `financial_summary.json.financials.{최신년도}.net_debt × -1` (net_debt 음수면 순현금, 양수면 순차입).
- [ ] **B7. EBITDA 5년 테이블**: `analysis.json.financials.rows["EBITDA"]` == `financial_summary.json.financials.{각년도}.ebitda` (단위 조원 변환 후, ±3% 이내).
- [ ] **B8. EPS 5년 테이블**: `analysis.json.financials.rows["EPS"]` == `financial_summary.json.financials.{각년도}.eps` (±2% 이내).
- [ ] **B9. 신용등급 정확성**: `analysis.json` 어디든 "Moody's/S&P/한국기업평가/NICE" 언급이 있으면 `data_dart_reports.json[0].sections.사업의_내용` 의 "신용평가에 관한 사항" 원문과 **정확히 일치** (Aa2/A1 같은 노치 오기 금지).
- [ ] **B10. R/R 내부 일관성**: `opinion.risk_reward` == `s01_opinion` 서술 == `s12_scenarios` 실제 계산 (하방 % / 상방 % 재계산하여 3곳 일치 확인). **삼성 1:2.4 vs 1:0.8 모순 재발 방지**.
- [ ] **B11. Wisereport 컨센서스 수치 정합성 (v4.8 신설)**: `analysis.json.sections.s02/s08/s16` 에 기재된 Forward 수치(2026E OP/순이익/EPS) 가 `data/{종목명}/_wisereport.json.financials_annual.{2026E}` 와 ±10% 이내 일치. **삼성 83.5조 vs 301.62조 사고(3.6배 오차) 재발 방지**. WebSearch 구 리포트 기반으로 Wisereport 값을 "이상치" 라 거절했을 경우 FAIL.

**검증 방법 -- 자동 스크립트 3개 실행 (v4.10 D 블록 추가)**

```bash
# B 블록 -- 수치 정합성 (B1~B11)
python scripts/verify_numbers.py {종목명}

# C 블록 -- 서술 품질 자동 검증 (v4.9 신설, C1~C6)
python scripts/verify_style.py {종목명}

# D 블록 -- 팩트 체크 자동 검증 (v4.10 신설, D1~D4)
python scripts/verify_facts.py {종목명}
```

**D 블록 (팩트 체크) 상세** -- v4.10 신설 (삼성전자 사업보고서 허위 인용 3건 사고 후):
- **D1. 사업보고서 직접 인용 실존 여부** -- 리포트의 "사업보고서 [0]에서 '...'라고 기재" 패턴을 추출하여 `data_dart_reports.json[0].sections` 원문에 키워드 60% 이상 매칭되는지 확인. **허위 인용 원천 차단**.
- **D2. 외부 출처 인용 리스트** -- UBS/트렌드포스/WSTS/노무라 등 외부 출처 + 수치 claim을 섹션별로 추출. 리포트 생성 후 사용자가 교차 검증할 수 있게 목록 제공.
- **D3. 증권사 목표가 테이블 정합성** -- s16 테이블의 증권사명/목표주가가 `financial_summary.json.consensus.brokers` 원본과 5% 이내 일치.
- **D4. Peer 테이블 정합성** -- s05/peers 테이블의 PER/시총이 `_peer_snapshot.json`과 10% 이내 일치.

**D 블록 FAIL 발생 시 메인 에이전트는 즉시 수정 필수.** 팩트 허위 인용은 리포트 신뢰성의 근간을 무너뜨리므로 CRITICAL로 분류.

**C 블록 (서술 품질) 상세**:
- **C1. 불릿 비율** < 30% (섹션당 전체 줄 대비 불릿 줄 비율)
- **C2. 전문용어 풀어쓰기** -- OPM/EBITDA/HBM/WACC/PMI/ROE/ROIC/PSR 첫 등장 시 괄호 풀이 필수
- **C3. 소제목 메시지성** -- "재무분석" 같은 단순 라벨 금지, 15자+ 서술형 강제
- **C4. 서술 톤** -- "것으로 보인다/추정된다/할 수도 있다" 금지, 단정적 어미
- **C5. 3요소 밀도** -- 섹션당 숫자 3개+ + 시사점 키워드("따라서/결과적으로/시사점/의미한다" 등) 2개+
- **C6. 자연어 비중** > 60% (테이블/코드 제외)

검증 대상 섹션: s02, s03, s04, s08, s09, s13, s14 (서술 중심 7개)

**C 블록 점수 < 70점이면 재작성 권고.** 최종 점수는 B + C 합산으로 종합 품질 지표.

스크립트는 `scripts/analysis_{종목명}.json` 와 `data/{종목명}/data_kis.json`, `financial_summary.json`, `data_dart_reports.json` 을 자동 로드하여 B1~B10 10개 항목을 PASS/FAIL/AMBIGUOUS 로 판정 출력한다.

- **PASS**: 원본과 정합. 통과.
- **FAIL**: 원본과 불일치. **반드시 수정**. 스크립트 종료 코드 2 반환 → LLM은 이 종목을 2회차로 넘기지 말고 먼저 수정할 것.
- **AMBIGUOUS**: 서술문에 매수/매도 키워드가 모두 등장하여 부호 판별 애매. 사람이 수동 확인 필요.

**스크립트 내부 로직 요약 (참고용)**:
- B1~B4: `analysis.json.price.*` vs `data_kis.json.current_price.*` 정확 일치 ±1% 이내
- B5: 각 투자자 20일 순매수 부호(±) == s17 서술의 "매도/매수" 키워드 방향 일치
- B6: `net_debt < 0` 이면 "순현금 X조" 서술 존재 확인 (X는 abs(net_debt)/10000 조)
- B7~B8: `financials.rows` 의 EBITDA/EPS 각 연도가 `financial_summary.json.financials.{year}.{ebitda,eps}` 와 ±2~3% 이내
- B9: `data_dart_reports.json[0]` "사업의_내용" 의 "Moody's ... 등급은 Xx" 원문 추출 → 리포트 전체 서술에 정확한 등급 문자열 존재 확인
- B10: (target_base − current) / (current − target_bear) 재계산 → `opinion.risk_reward` 문자열에 포함 확인

**사고 재발 방지 효과**: 삼성전자 v1 report-critic 13건 결함 중 8건이 이 스크립트로 사전 자동 차단 가능 (A 수급 부호, C 순현금, D 신용등급, F R/R, G 배당 DPS 간접, H EBITDA, I EPS, 52주저 B4).

점수: __/100 (A 8개 × 7점 = 56 + B 10개 × 4.4점 = 44)
문제점 (최소 3개): (A/B 모두에서 발견. 수치 정합성 위반은 무조건 "치명적"으로 분류)
수정 항목: (목록)
```

→ A/B 문제점 모두 수정 후 **Step 1c (서브에이전트 호출)** 로 진입.

---

### 🔹 Step 1c: `report-critic` 서브에이전트 호출 (Fresh Context 가혹 비평)

**메인 에이전트가 자기 리포트를 비평하면 자기 일관성 편향 때문에 결함을 못 찾는다.** 대한항공 v1 세션에서 증명됨 (체크리스트 3회 = 결함 0개, 사용자 한 문장 도전 = 5개 치명적 결함). 해결책: **리포트를 처음 보는 서브에이전트**가 Fresh context로 비평한다.

**호출 방법**:

```
Agent({
  description: "{종목명} 리포트 가혹 비평",
  subagent_type: "report-critic",
  prompt: """
    {종목명} 리포트 STEP 6 1회차 Step 1c 검증 요청.

    비평 대상:
    - 리포트 JSON: scripts/analysis_{종목명}.json
    - PDF: output/{종목명}/report_{종목명}_상세.pdf

    교차 검증용 원본 데이터:
    - data/{종목명}/financial_summary.json
    - data/{종목명}/_peer_snapshot.json
    - data/{종목명}/_per_band.json
    - data/{종목명}/data_dart_reports.json
    - data/{종목명}/data_kis.json

    최고의 애널리스트 관점에서 냉정하고 객관적으로 평가해줘.
    더 수정할 내용 없어?
  """
})
```

**⚠️ 프롬프트 원칙**: 마지막 한 문장("최고의 애널리스트 관점에서 냉정하고 객관적으로 평가해줘. 더 수정할 내용 없어?")을 순수하게 유지한다. "5가지 관점을 찾아라", "DCF 가정 체크하라" 같은 가이드를 추가하면 서브에이전트가 체크리스트 모드로 전환되어 효과가 사라진다. 서브에이전트의 시스템 프롬프트(`.claude/agents/report-critic.md`)에 이미 페르소나와 원칙이 모두 정의되어 있으므로, 호출 프롬프트는 **파일 경로 + 한 문장 질문**이면 충분하다.

서브에이전트가 결함 목록을 반환하면 → 메인 에이전트가 수정 (Step 1d) → `generate_all.py` 재실행 → **종료 판정**.

---

### 🔹 2회차 (조건부): 서브에이전트 재호출

**2회차는 기본적으로 실행하지 않는다.** 아래 조건 중 하나를 충족할 때만 실행한다:

1. **Step 1c 서브에이전트가 CRITICAL 등급 결함 3건 이상 발견** (HIGH 등급 포함 5건 이상)
2. **Step 1a B 블록에서 FAIL 이 4건 이상** (수치 정합성 대규모 붕괴)
3. **사용자가 명시적으로 "더 수정할 내용 없어?" 재검증 요구**

위 조건 중 하나라도 해당되면, Step 1d 수정 완료 후 서브에이전트를 **1회 더** 호출하여 수정 검증한다. 프롬프트는 Step 1c와 동일하되 "1회차에서 CRITICAL 3건+ 수정 반영한 상태. 새 결함 있는지 재검증" 문구 추가.

**2회차 결과**:
- 신규 CRITICAL 결함 발견 시 → 수정 후 종료 (3회차 금지)
- 잔존 결함 없음 → "리포트 최종 완성" 선언

**왜 조건부인가?** 무조건 2회차는 대부분 "한계효용 낮은 재확인"에 그친다. 실측상 Step 1c 서브에이전트가 치명 결함 10건 이상을 잡은 삼성전자 케이스에서도, 3회차 재호출이 추가로 잡은 결함은 평균 1~2건 (추정)에 불과했다. 조건부 실행으로 평균 비용을 60% 절감하면서도 품질 저하는 거의 없다.

---

### 사이클 실행 순서 요약 (v4.6)

1. **Step 1a**: `python scripts/verify_numbers.py {종목명}` 실행 → B1~B10 자동 검증 → FAIL 수정
2. **Step 1b**: 메인 에이전트 체크리스트 A 8개 → 빠진 구조 식별 → 수정
3. **Step 1c**: `report-critic` 서브에이전트 호출 → 질적 비평 결과 수신
4. **Step 1d**: Step 1a/1b/1c 결과 **모두** 반영하여 analysis.json 수정 → `generate_all.py` 재실행
5. **종료 판정**:
   - Step 1c CRITICAL 3건 미만 + Step 1a FAIL 4건 미만 → **"리포트 최종 완성" 선언 (종료)**
   - Step 1c CRITICAL 3건+ 또는 Step 1a FAIL 4건+ → **2회차 조건부 실행** (서브에이전트 재호출 1회)
6. **모든 단계 결과를 사용자에게 보여준다.** 서브에이전트 보고는 요약만 하지 말고 원문을 그대로 전달한다.

**3회차는 어떤 조건에서도 실행하지 않는다** (v4.5 이전과의 결정적 차이).

**왜 서브에이전트인가 — 실증 근거**:

| 방식 | 대한항공 v1 결과 | 원인 |
|---|---|---|
| 메인 체크리스트 3회 | 결함 **0개** | 자기 일관성 편향, 체크리스트 범위 한정 |
| 사용자 한 문장 도전 | 5개 치명적 결함 | Fresh context + 순수 자연어 |
| 스킬 v4.1에서 `code-reviewer` 서브에이전트 호출 (이번 세션 스킬 버그 사냥) | 4개 CRITICAL + 3개 HIGH + 2개 MEDIUM | Fresh context + 독립 판단 |

**서브에이전트는 "사용자 도전"을 구조적으로 자동화한 것**이다. 사용자가 매번 "더 수정할 내용 없어?"라고 물어야 발견되던 결함을, 서브에이전트가 자동으로 찾는다. 사용자 개입 의존성 제거가 핵심 가치.

**이 규칙을 어기면 JYP v1, 에코프로 v1, 대한항공 v1 같은 사고(시총 10배, Peer 추정, 역사 밴드 추정, 글로벌 FSC 날조, SOTP 부재)가 재발한다.**

**서브에이전트 정의 파일**: `.claude/agents/report-critic.md` — 이 파일에 월가 시니어 페르소나, 검증 원칙, 강제 발굴 프로토콜이 전부 정의되어 있다. 호출 프롬프트는 짧게 유지하고 시스템 프롬프트는 서브에이전트 파일에 맡긴다.

---

## 리포트 21개 섹션

```
[목차] + [핵심 실적 추정 테이블]
 1. 투자의견 & 목표주가 (BUY/HOLD/SELL)
 2. 투자포인트 (4줄 구조 × 3~5 포인트)
 3. 회사 개요 / 비즈니스 모델 (10개 요소 필수)
 4. 산업 & 시장 + 밸류체인 (8개 요소 필수)
 5. 경쟁 구도 + Peer Comparison
 6. 경제적 해자
 7. 경영진 + 내부자 매매
 8. 재무 분석 + Forward 추정 + 사업보고서 인용
 9. 밸류에이션 (DCF/상대/밴드/민감도 — Q1/Q2/Q3 필수)
10. 매크로 리스크
11. 카탈리스트 타임라인
12. Bear/Base/Bull + Risk-Reward
13. 투자 논문 + 3대 근거
14. 숏 논거 3가지 + 반박
15. 실적 Beat/Miss
16. 애널리스트 컨센서스 (KR)
17. 수급 분석 (KR)
18. 주주환원
19. Trust / Worry / Watch
20. 실행 계획 (진입/손절/목표/분할매수)
21. 분석 신뢰도 & 한계
```

---

## 품질 체크리스트 (generate_all.py 실행 전 반드시)

### 데이터 정합성 (가장 중요)
- [ ] **EPS 일치**: JSON.price.eps = KIS EPS = 현재가÷PER
- [ ] **확정 vs 추정**: 2023~2025 확정, 2026E~ 컨센서스
- [ ] **사업부 비중 출처 정확**: DART 실제면 "DART [0]", 추정이면 "추정" 명시
- [ ] **현재가 정확**: KIS API 값 사용 (웹 검색 금지)
- [ ] **market_cap_num 단위**: 반드시 억 단위 정수

### v4 핵심 체크 (s02/s03/s04)
- [ ] STEP 2.5 Grep 10개 패턴 모두 수행
- [ ] s02 4줄 구조 엄수 (주장/근거/왜주가/시장간과)
- [ ] s03 지역별 매출 테이블 존재
- [ ] s03 신용등급 궤적 표 존재 (있는 경우)
- [ ] s04 본 종목 글로벌 M/S + 지역별 M/S 명시
- [ ] 사업보고서 직접 인용 5곳 이상 (전체 리포트)
- [ ] 전년 대비 변화점 최소 3개

### 분석 깊이
- [ ] YoY 20%+ 변동 항목에 "왜?" 3단계
- [ ] R/R 1:2 이상 (미만이면 BUY 금지)
- [ ] "사지 말아야 할 이유" 3개 + 데이터 반박
- [ ] 업종별 KPI 테이블 (자동차/반도체/바이오/지주/리츠)

### 퀀트 프로토콜
- [ ] Q1 DCF 가정 테이블 (WACC/g/Beta/Rf/ERP/Kd)
- [ ] Q2 민감도 테이블
- [ ] Q3 역사 밴드 + 백분위
- [ ] Q4 컨센서스 델타
- [ ] Q5 적자기업 대체 지표 (해당 시)
- [ ] Q6 Q4 일회성 정상화 (해당 시)
- [ ] Q7 Peer 선정 근거 한 문장
- [ ] Q8 수급 맥락화 (유동주식 %)
- [ ] Q9 변동성·베타 포지션
- [ ] Q10 Forward 6년 테이블

### 출력물 (v3)
- [ ] `output/{종목명}/report_{종목명}_상세.pdf` 단일 파일 생성 (1.5~2MB)
- [ ] 페이지 수 24~50 범위 (분량 적절)
- [ ] Cover 1p + Executive 1p + 21섹션 자연 흐름 + Final Call 1p 구조
- [ ] 마크다운 raw 기호 (`**`, `###`, `---`, `>`, `-`) 본문에 노출 안 됨
- [ ] 이모지 (🔴🟢⚠️📊 등) 본문에 노출 안 됨 (★☆ 별점만 보존)
- [ ] 표는 NYT 스타일 (네이비 상하 보더, tabular-nums, 우측 정렬)
- [ ] 페이지 하단 빈 공간 거의 없음 (자연 흐름)
